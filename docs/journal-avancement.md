# Journal d'avancement — Mothana

Historique détaillé des sujets **terminés** : décisions techniques, bugs trouvés en testant et corrigés, PR associées. Extrait de `CLAUDE.md` le 2026-08-09 pour l'alléger — ce fichier n'est **pas** lu automatiquement à chaque session, seulement à la demande (ex. pour retrouver pourquoi une décision passée a été prise, ou avant de retoucher une zone déjà creusée par le passé).

Pour l'état actif (sujets en cours/à faire), voir `CLAUDE.md` → section "État d'avancement".

---

## ✅ MVP + Post-MVP terminés

- Étapes 0–9 : toutes complètes
- Gestion comptes admin (Edge Functions `create-admin`, `disable-admin`, `get_org_admins`)
- Nouveaux champs participant (civilite, adresse, nom2/prenom2) dans formulaires, fiches, reçus PDF
- Import 3336 participants réels (Wat Velouvanaram)
- Pagination UI + `fetchAllRows` (résout limite 1000 lignes PostgREST)
- `ParticipantAutocomplete` dans `DonModal`
- Accessibilité modales (`Modal.tsx`, `useFocusTrap.ts`, `modalStack.ts`)
- Mises à jour optimistes + toasts
- `vercel.json` SPA rewrite
- Suppression participant (policy RLS DELETE `profils_participant_delete.sql`)
- Activités : `id_externe` + `date_debut`/`date_fin`

## ✅ Priorité 1 : Refonte Cerfa — terminée (2026-07-18)

Voir `docs/brief-cerfa.md` pour le brief technique complet. Les 6 étapes sont en production. Détail de chaque étape :

1. ✅ **Migrations SQL** (brief §1) — exécutées en production le 2026-07-17 :
   - Colonnes adresse sur `organisations` (`organisations_adresse_fiscale.sql`)
   - Table `templates_recu` (`templates_recu.sql`) — trigger `updated_at` ajouté en plus du brief, cohérent avec les autres tables
   - Champs `numero_ordre`, `type_cerfa`, `snapshot_donateur`, `snapshot_organisation`, `template_id`, `email_envoye_at` sur `recus_fiscaux` (`recus_fiscaux_cerfa_fields.sql`)
   - Fonction SQL `next_numero_recu()` (`next_numero_recu.sql`), numérotation atomique par séquence PostgreSQL dédiée par org/année — testée sur Wat Velouvanaram (`2026-001`), séquence remise à zéro après le test (`is_called: false`) pour ne pas brûler le premier numéro réel
   - ✅ Chevauchement avec les données existantes de `modele_recu_pdf` résolu : seule `adresse` (chaîne combinée "rue, CP Ville") avait une vraie donnée (Wat Velouvanaram) — backfillée vers les colonnes structurées via `organisations_backfill_adresse.sql` (regex sur le CP à 5 chiffres). `siret` (valeur placeholder `"..."`) et `objet_association` (vide) n'avaient pas de donnée réelle exploitable : pas de backfill, seront simplement ressaisis en `rna`/`siren`/`objet_social` à l'étape 2

2. ✅ **Paramètres organisation** (brief §5) — mergé sur `main` le 2026-07-17 (PR #16 → `feat/cerfa-migrations`, puis PR #17 → `main`) :
   - Section "Informations fiscales" de `ParametresPage.tsx` refondue : adresse structurée (`organisations.adresse`/`code_postal`/`ville`/`pays`), RNA, SIREN, objet social, mention légale (pré-remplie), numéro du premier reçu, taux de réduction fiscale (défaut 66%, éditable pour les orgs à 75%) — tous dans `modele_recu_pdf` JSONB sauf l'adresse
   - Remplace l'ancien modèle `siret`/`objet_association`/`mentions_complementaires` (aucune donnée réelle en prod hors adresse déjà migrée en étape 1, confirmé par requête directe avant la refonte)
   - Bannière d'obligations légales affichée (conservation 6 ans, déclaration article 222 bis CGI, amende 66%)
   - Testé manuellement par l'utilisateur (saisie, sauvegarde, persistance DB) — confirmé OK

3. ✅ **Templates HTML par défaut** (brief §3) — code sur `feat/cerfa-templates-defaut`, migration RLS exécutée en production le 2026-07-17 :
   - Deux templates conformes Cerfa dans `src/lib/defaultCerfaTemplates.ts` (11580 particuliers articles 200/200 bis CGI, 16216 entreprises article 238 bis CGI) — placeholders `{{variable}}` selon liste du brief §2.2, CSS partagé A4 imprimable
   - Seedés automatiquement dans `SuperAdminPage.tsx` (`OrgModal.handleSubmit`) à la création d'une organisation : insert `organisations` avec `.select('id')` pour récupérer l'id, puis insert des 2 lignes `templates_recu` (`is_active: true`)
   - Migration `templates_recu_super_admin_bypass.sql` : la policy RLS `templates_recu_org` n'avait pas le bypass super-admin (table créée après `super_admin_rls.sql`) — sans elle le seed échouait car le super-admin n'est pas encore membre de l'organisation qu'il vient de créer (`current_effective_organisation_id()` renvoie NULL)
   - Rendu visuel vérifié via Playwright headless (screenshot des 2 templates avec données d'exemple) — pas de test de création d'organisation réelle en production pour éviter de polluer les données (à valider par l'utilisateur)
   - Bug bloquant trouvé par l'utilisateur en testant cette PR (génération de reçu Wat Strasbourg → erreur serveur), corrigé dans cette même PR plutôt qu'à part : `generate-recu` utilisait encore l'ancien `mode_paiement` texte alors qu'il est numérique depuis la PR #14 — `pdf-lib` recevait un nombre au lieu d'une chaîne et plantait. Corrigé, déployé en production, testé OK par l'utilisateur

4. ✅ **Refonte Edge Function `generate-recu`** (brief §2) — codée sur `feat/cerfa-generate-recu-gotenberg`, testée bout-en-bout en production le 2026-07-18 :
   - `pdf-lib` abandonné, remplacé par Gotenberg (déployé sur Railway, secret `GOTENBERG_URL` configuré) — conversion HTML→PDF via `POST /forms/chromium/convert/html`, un seul fichier `index.html` avec le CSS inliné en `<style>` (pas de fichiers séparés)
   - Nouveau flux complet (brief §2.3) : validation organisation → validation participant → détermination type_cerfa → template actif → `next_numero_recu()` (conservé si régénération) → snapshots donateur/organisation → placeholders → PDF → Storage (`{org}/{année}/{numero_ordre}.pdf`) → upsert `recus_fiscaux`
   - Validations `regles-recus-fiscaux.md` §2-3 implémentées côté serveur (source de vérité — l'étape 5 les rendra aussi visibles côté UI avant le clic) : organisation (adresse/CP/ville, RNA ou SIREN, objet social, mention légale) puis participant (nom/adresse/CP/ville/civilité, prénom selon civilité, blocage dur sur civilité Famille ou NULL). Réponses 422 avec message clair + `missing_fields`
   - Règles de formatage du nom du donateur (brief §4) implémentées et vérifiées (`M. Jean DUPONT`, `M. et Mme Jean DUPONT`, raison sociale seule pour société/association) — testées unitairement avec `deno run`
   - Taux de réduction : `taux_reduction` configurable de l'organisation (défaut 66%) pour le 11580, **60% fixe** pour le 16216 (régime IS entreprises, non configurable)
   - Backfill `templates_recu_backfill_orgs_existantes.sql` : Wat Velouvanaram/Strasbourg/Choisy créées avant l'étape 3 n'avaient aucun template, généré depuis les mêmes constantes que `defaultCerfaTemplates.ts` (évite toute divergence)
   - Bug trouvé et corrigé pendant le test réel : le template 11580 concaténait `{{donateur_civilite}}` et `{{donateur_nom_complet}}` (qui inclut déjà le titre de civilité) → "Monsieur M. Nicolas BOULOM" en double. Corrigé dans `defaultCerfaTemplates.ts` + migration `templates_recu_fix_donateur_civilite_duplication.sql` pour les templates déjà en base
   - Testé bout-en-bout par l'utilisateur sur Wat Strasbourg : blocage organisation incomplète ✅, champ retiré de la liste une fois rempli ✅, blocage participant incomplet ✅, génération réelle avec PDF vérifié (rendu, montant en chiffres/lettres, numéro d'ordre conservé en régénération) ✅

5. ✅ **Évolutions UI page Reçus fiscaux** (brief §6) — codée sur `feat/cerfa-recus-fiscaux-ui`, build/lint/typecheck OK, smoke test navigateur fait (pas d'identifiants admin pour tester le rendu réel avec données) :
   - `src/lib/cerfaValidation.ts` : logique de validation organisation/participant dupliquée côté client (mêmes règles que `generate-recu`, le backend reste la source de vérité) — `validateOrganisationCerfa()` et `validateParticipantCerfa()`
   - Bannière de blocage si paramètres organisation incomplets, avec lien direct vers `/admin/parametres`
   - Icône ⚠️ à côté du nom + message détaillé sous le statut, avec tooltip/texte listant les champs manquants ou la raison du blocage (civilité Famille/NULL)
   - Colonnes N° reçu et Type (libellé "11580 · Particuliers" / "16216 · Entreprises") ajoutées au tableau
   - CTA Générer/Regénérer **désactivé** quand l'organisation ou le participant est bloqué (décision utilisateur du 2026-07-18) — "Générer tous" filtre aussi les lignes bloquées plutôt que d'échouer dessus
   - Bouton "Modifier le participant" sous le message d'erreur, ouvre `ParticipantModal` directement depuis cette page (décision utilisateur du 2026-07-18) — réutilise le composant existant de `ParticipantsPage`
   - Confirmation avant regénération (modale réutilisant le composant `Modal` existant, même pattern que la suppression de participant), précise que le numéro d'ordre est conservé
   - Toast de succès après génération réussie (décision utilisateur du 2026-07-18), réutilise `useToast`/`Toast` déjà utilisés sur `ParticipantsPage`
   - `RecuFiscal` (types/index.ts) étendu avec `numero_ordre`/`type_cerfa`, présents en base depuis l'étape 1 mais jamais exposés côté frontend
   - Testé par l'utilisateur : bannière, icônes/tooltips, CTA désactivés, toast — tous OK. Bug UI trouvé sur `ParticipantModal` (bouton "Modifier le participant" ouvre une modale dont les boutons Annuler/Enregistrer n'étaient pas visibles sans scroll) : corrigé — corps scrollable + footer sticky avec ombre indicative et coins bas arrondis (`rounded-b-2xl`, sinon le fond opaque du footer recouvrait l'arrondi du conteneur `Modal`)

6. ✅ **Gestion des templates** dans Paramètres (brief §7) — mergé sur `main` le 2026-07-18 (PR #25), dernière étape de la refonte Cerfa (priorité 1) :
   - Nouvelle dépendance `@monaco-editor/react` (aucun éditeur de code n'existait dans le projet)
   - Nouvelle section "Modèles de reçus fiscaux" dans Paramètres (`TemplatesRecuSection.tsx`) : liste des templates groupés par type (11580/16216), badge Actif/Inactif/Archivé
   - `TemplateRecuEditorModal.tsx` : création **et modification** d'un template (nom, type, éditeur Monaco HTML/CSS avec onglets, aperçu iframe live) — création désactivée par défaut (à activer explicitement) ; modification ajoutée en cours de PR (sans elle, un template déjà utilisé pour générer un reçu ne pouvait plus jamais être ni corrigé ni supprimé)
   - Liste complète des 19 placeholders disponibles affichée sous l'éditeur (tags cliquables, copie presse-papier, exemple de valeur au survol) — ajoutée en cours de PR suite à un retour utilisateur
   - `TemplateRecuPreviewModal.tsx` : aperçu en lecture seule d'un template existant
   - `src/lib/cerfaPreview.ts` : données d'exemple + rendu HTML partagés entre les deux modales (mêmes placeholders que `generate-recu`)
   - Activer : désactive l'ancien template actif du même type puis active le nouveau (deux updates séquentiels, pas de transaction SQL — risque de concurrence jugé acceptable pour une action admin mono-utilisateur)
   - Archiver le template actif : confirmation spécifique avertissant que la génération sera bloquée pour ce type tant qu'un autre template n'est pas activé
   - Supprimer : vérifie d'abord qu'aucun `recus_fiscaux.template_id` ne référence le template (bloque avec message si utilisé), puis confirmation standard avant suppression définitive
   - Monaco testé et fonctionnel (coloration syntaxique HTML/CSS, onglets, aperçu live, mode édition pré-rempli) via une route de test temporaire ajoutée puis retirée avant chaque commit — testé et validé par l'utilisateur, PR mergée

**Incident opérationnel identifié et corrigé après cette étape** : l'agent tuait par erreur l'instance `npm run dev` permanente de l'utilisateur (exposée réseau, utilisée pour piloter le travail à distance) via des `pkill -f vite` pendant les tests visuels. Règle ajoutée dans `CLAUDE.md` ("Environnement de développement", toujours active) + mémoire persistante : ne plus jamais tuer de processus par pattern de nom, réutiliser l'instance existante sur le port 5173.

## Priorité 2 — Export comptable

- ✅ Export CSV dons (2026-07-22) — bouton "Exporter" sur `DonsPage`, exporte `filteredDons` (période/participant/activité/mode déjà filtrés à l'écran), délimiteur `;` + BOM UTF-8 pour Excel FR (`src/lib/csvExport.ts`, réutilisable pour les prochains exports). Au passage : filtre "Participant" remplacé par `ParticipantAutocomplete` (recherche nom/prénom, même composant que `DonModal`)
- ✅ Tableau de bord comptable (2026-07-23) — nouvelle page `/admin/comptabilite` (`ComptabilitePage.tsx`) : courbe mensuelle N vs N-1, répartition par activité, répartition par mode de paiement, cartes de stats. Recharts (nouvelle dépendance), palette/formes conformes au skill `dataviz` (validée via `scripts/validate_palette.js`)
- ✅ Récapitulatif déclaratif article 222 bis CGI (2026-07-25) — mergé sur `main` (PR #30), après clarification réglementaire : la déclaration ne nécessite pas de format d'export vers un portail externe, seulement deux chiffres agrégés par organisation/année (nombre de reçus émis + montant total) à recopier manuellement dans la télédéclaration officielle. Carte "Récapitulatif déclaratif" sur `ComptabilitePage.tsx` (`DeclarationCerfaCard.tsx`), une ligne par année, bouton copier + export CSV, agrégation directe sur `recus_fiscaux` (`GROUP BY annee`). Checkpoint agrégation validé en prod avant codage UI : `recus_fiscaux` a une contrainte `UNIQUE (profil_participant_id, annee)` et `generate-recu` fait un upsert dessus — pas de double-comptage possible sur les reçus régénérés. Au passage : extraction de `src/lib/clipboard.ts` (pattern `execCommand('copy')` déjà dupliqué dans `TemplateRecuEditorModal.tsx`)
- Rapprochement chèques/virements — abandonné (2026-08-17), carte Trello archivée. Jamais cadré (le moins défini des 4 items), et pas rattaché à un vrai besoin utilisateur exprimé — pas d'obligation légale pour une petite association (pas de commissaire aux comptes), et connexion au compte bancaire d'une asso jugée hors de portée réaliste du produit

**Import des dons — rapport d'erreurs écarté (2026-07-23)** : discuté (export CSV des lignes en erreur de `ImportWizard.tsx`), mais décision finale de l'utilisateur de ne rien construire — les lignes incorrectes restent simplement ignorées lors de l'import

## ✅ Priorité 3 : Wizard de template Cerfa — terminée (2026-07-20)

- Upload PDF modèle → analyse Claude Vision (`generate-template-from-pdf`, modèle `claude-haiku-4-5` — coût/qualité jugés équivalents à Sonnet 5 sur cette tâche par l'utilisateur) → brouillon HTML/CSS via tool use forcé → toujours ouvert dans l'éditeur Monaco existant, jamais activé automatiquement
- Placeholders obligatoires mis en avant dans l'éditeur (`src/lib/cerfaPreview.ts` : `CERFA_MANDATORY_KEYS`, `CERFA_RNA_SIREN_GROUP`, `getMissingMandatoryPlaceholders()`) — activation d'un template bloquée tant qu'ils ne sont pas tous présents
- CTA "Placeholders" en popover (zone Annuler/Enregistrer), copie au clic (avec repli `execCommand('copy')` car `navigator.clipboard` est indisponible sur l'URL réseau HTTP utilisée pour piloter l'instance de dev à distance)
- Confort d'édition ajouté au passage : plein écran (`Modal.tsx` — prop `fullScreen` générique + `heightClassName`), sélecteur Les deux/Éditeur/Aperçu, Ctrl/Cmd+S
- Éditeur WYSIWYG (GrapesJS pressenti) volontairement écarté pour l'instant — à reconsidérer seulement si l'édition Monaco devient une vraie friction
- ✅ **Assets par organisation (liste ouverte, n assets) + placeholders président** — cadré le 2026-08-01, redesigné et mergé le 2026-08-03 (**PR #35**). Version initiale codée avec 3 slots fixes (`logo_url`/`tampon_url`/`signature_url`) ; retravaillée à la demande de l'utilisateur en une liste extensible avant merge. Décisions : bucket Storage `organisation-assets` **public** (ces images finissent de toute façon sur un PDF remis au donateur, évite les URLs signées pour l'aperçu Monaco et pour Gotenberg) ; les 2 templates Cerfa par défaut (11580/16216) **non modifiés**. Nouvelle table `organisation_assets` (`organisation_id`, `identifiant` slug unique par org, `libelle`, `url`) — chaque asset devient un placeholder `{{asset_<identifiant>}}` résolu dynamiquement par organisation (éditeur/aperçu Monaco, `generate-recu`, `snapshot_organisation.assets`). Sous-section "Identité visuelle" dans `ParametresPage.tsx` : ajout/remplacement/suppression d'un nombre illimité d'assets (libellé + fichier PNG/JPEG 2 Mo max, upload + sauvegarde DB immédiats), plus nom/titre du président (`{{president_nom}}`/`{{president_titre}}`, désormais résolus avec la vraie valeur de l'organisation dans l'aperçu de template, pas une donnée d'exemple générique — bug trouvé et corrigé pendant le test). **Bug bloquant trouvé et corrigé pendant le test** : le code de `generate-recu` avait été mis à jour dans le repo mais jamais redéployé sur Supabase (dernier déploiement du 2026-07-18) — les placeholders fonctionnaient dans l'aperçu (calculé côté frontend) mais pas dans le PDF réellement généré (edge function obsolète). Redéployé (`supabase functions deploy generate-recu`, version 9). ⚠️ Point de vigilance : penser à redéployer les edge functions après tout changement de leur code, le déploiement n'est pas automatique au push/merge sur ce projet (voir mémoire persistante)

## Backlog non priorisé (ajouté 2026-07-25) — les 5 items terminés le 2026-07-25

- ✅ **Affichage cassé des dons d'un participant** (page Participants) — PR #31 mergée. Cause réelle du "cassage" : `h-full` ne se résout pas quand le parent n'a qu'un `max-height` sans `height` explicite (panneau latéral + modale) — repris le pattern `flex-1 overflow-hidden` + footer `shrink-0` déjà validé dans `ParticipantModal.tsx`. Décision finale après itération : pas de modale séparée "voir plus", la petite fenêtre sticky d'origine suffit une fois le scroll interne corrigé — tous les dons y restent listés directement. Bascule desktop/mobile abaissée de `lg` (1024px) à `md` (768px), le seuil `lg` faisait passer en tiroir plein écran une simple fenêtre de navigateur non maximisée
- ✅ **Lenteur de chargement sur Wat Velouvanaram** — PR #32 mergée, cause identifiée avec certitude (pas de simple hypothèse) via `supabase db advisors --linked` : `auth_rls_initplan` sur ~20 policies (auth.jwt()/auth.uid() ré-évalués à chaque ligne scannée plutôt qu'une fois par requête — piège RLS documenté par Supabase). Chiffres réels vérifiés en prod avant tout correctif : Wat Velouvanaram = 3335 participants / **15632 dons** / 314 activités, les 2 autres organisations n'ont qu'1 ligne de test chacune (l'hypothèse initiale d'index manquant sur `activites`/`profils_participant` a été invalidée par l'advisor + `pg_indexes` — un index composite couvrant existait déjà via les contraintes uniques `id_externe`). Policies RLS + fonctions helper réécrites avec le pattern `(select ...)` recommandé par Supabase (migration `rls_auth_initplan_perf.sql`, exécutée directement en prod via `supabase db query --linked` après confirmation explicite, vérifiée par re-run de l'advisor : 0 warning restant, aucune régression de policy, aucune nouvelle alerte sécurité). Complété par la parallélisation de `fetchAllRows` (pagination par lots de 4 requêtes en parallèle au lieu d'une par une — 16 pages séquentielles → 4 vagues pour les dons). Résultat confirmé par l'utilisateur : Activités instantané, Participants/Dons nettement plus courts (Dons reste un peu plus long, cohérent avec son volume de lignes)
- ✅ **Champ de recherche pour les activités** — PR #33 mergée, `ActiviteAutocomplete.tsx` (même pattern que `ParticipantAutocomplete`) remplace le `<select>` dans `DonModal`, le filtre de `DonsPage`, et `BenevolePage`
- ✅ **Page Activités : filtre + pagination** — PR #33 mergée (même PR que le point précédent, même surface de code), recherche + pagination (50/page) ajoutées, en-tête aligné sur `ParticipantsPage`. Fetch passé à `fetchAllRows` (protection contre une troncature silencieuse au-delà de 1000 activités)
- ✅ **Réaffectation de don à un autre participant** — cadré puis codé le 2026-07-25. Décision produit finale : blocage total (pas d'avertissement + régénération manuelle) dès qu'un reçu fiscal a déjà été émis pour l'année du don, côté source **ou** destination — l'utilisateur a tranché en cours de cadrage pour la simplicité/sécurité plutôt que le palier intermédiaire initialement envisagé. Implémenté dans `DonModal.tsx` : champ participant déverrouillé en mode édition (`disabled={isEdit}` retiré), check `recus_fiscaux` (organisation_id + annee + profil_participant_id in [source, destination]) exécuté avant le submit si le participant a changé, message d'erreur explicite listant qui bloque si un reçu existe déjà. Validation uniquement côté client (pas d'Edge Function pour les dons, cohérent avec le reste du flux `DonModal`/`DonsPage`)

## ✅ Module Adhérents — V1 terminée le 2026-08-04

Cadré le 2026-08-03 ; étape 1 committée directement sur `main`, étapes 2 à 5 mergées via PR #36→#39 ; étape 6 explicitement hors scope. Contexte : besoin remonté par un tiers à l'utilisateur, distinct des participants/donateurs (`profils_participant`). Un adhérent n'est **pas** un rôle sur un profil existant mais une population à part entière, sans lien direct en base pour l'instant.

**Décisions structurantes actées :**
- **Refonte navigation** : l'accueil (aujourd'hui hub admin/bénévole) devient un vrai **dashboard organisation** post-connexion admin (stats dons/évènements récents, adhérents proches d'expiration, etc.), alimenté par les deux grandes sections. La mire de connexion devient la page d'accueil de l'app, avec lien vers l'espace bénévole
- **Regroupement** : dons, participants, évènements/activités, reçus fiscaux → section **"Dons"**. Nouvelle section **"Adhérents"** au même niveau, pas en sous-section
- **Historisation** : une adhésion = un cycle annuel dans une table `adhesions` séparée de `adherents` (même pattern qu'un don rattaché à un participant), pour permettre le suivi actif/expiré par année sans perdre l'historique
- **Civilité simplifiée** : enum dédié à 3 valeurs (0 non défini / 1 M. / 2 Mme), volontairement distinct de l'enum civilité à 7 valeurs des participants (pas de personne morale/famille adhérente pour l'instant)
- **Suppression** : soft delete (statut archivé) plutôt que hard delete, pour anticiper un futur lien avec les dons
- **Champs fournis par la base source existante**, arbitrés un par un entre `adherents` (propriétés stables de la personne) et `adhesions` (propriétés du cycle annuel) :
  - `Statuts_Acceptés` → `adherents.statuts_acceptes` (accepté une fois, stable dans le temps)
  - `Consent_RGPD` → `adherents.consent_rgpd` (concerne la personne, pas le cycle annuel)
  - `Bulletin_signé` → `adhesions.bulletin_signe` (peut être redemandé à chaque renouvellement)
  - `AG_Droit_vote` → `adhesions.droit_vote_ag` (conditionné à la cotisation de l'année en cours)
  - `Cotisation_annuelle` → `adhesions.montant_cotisation` (nullable, l'association n'a pas de montant fixe actuellement)
  - `Date_paie_cotisation` → `adhesions.date_paiement_cotisation`
  - `Mode_paie_Cotis` → `adhesions.mode_paiement` (enum Mothana existant réutilisé)
  - `Renouvellement` → `adhesions.renouvellement`, **calculé** (déduit d'une adhésion antérieure existante) plutôt que saisi, pour éviter une incohérence déclarative
  - `Recu_délivré` → **écarté**, champ vide sur toutes les lignes de la base source, aucun usage identifiable, pas de génération de document de reçu de cotisation prévue pour l'instant (à reconsidérer si le besoin émerge, réutiliserait l'infra Gotenberg comme les Cerfa)
- **Lien avec les participants/donateurs** : aucun lien direct en base pour la V1. Un adhérent peut être donateur ou non et inversement. Mécanisme futur envisagé (**prio basse, non développé**, reste dans le backlog actif) : à la saisie d'un don, recherche par nom/prénom d'abord côté participants, puis fallback sur la base adhérents si pas de match — risque de doublonnement entre les deux populations à traiter à ce moment-là, pas avant
- **Import** : réutilisation et généralisation du système d'import participants existant (mapping colonnes piloté par config plutôt que codé en dur). `id_externe` inclus dans l'import
- **Carte d'adhérent** : gabarit HTML/CSS éditable réutilisant l'infra existante (Gotenberg + éditeur Monaco de la refonte Cerfa), format **planche A4** avec plusieurs cartes positionnées pour faciliter la découpe (pas une carte par page) — sélection multiple d'adhérents pour impression par lot

**Formulaire d'adhésion (champs V1)** : date d'adhésion, civilité, nom, prénom, date de naissance, adresse, code postal, ville, téléphone, courriel — plus les champs de cycle listés ci-dessus

**Page liste** : filtrage par nom+prénom ou prénom+nom, actions modifier/supprimer (archiver) avec alertes appropriées

**Séquencement de développement :**

1. ✅ Migrations SQL `adherents` + `adhesions` (RLS, contraintes, `id_externe`) — exécutées en prod le 2026-08-03. Points vérifiés avant exécution :
   - `is_super_admin()` **n'existe pas** comme fonction dans ce projet (seule `current_effective_organisation_id()` existe, uuid sans argument) — les policies rédigées initialement avec `(select is_super_admin())` ont été corrigées avant exécution pour utiliser l'expression inline `((select auth.jwt()) -> 'app_metadata' ->> 'is_super_admin')::boolean = true`, le même pattern que `super_admin_rls.sql`/`rls_auth_initplan_perf.sql`
   - `pg_trgm` : confirmé non installée ailleurs sur ce projet, schéma `extensions` existant — activée sans souci
   - `mode_paiement` : confirmé `smallint` côté `dons`, contrainte `CHECK` à `[1,2,3,4]` (1=Espèces, 2=Chèque, 3=Prélèvement-virement, 4=Autres, cf. `src/lib/modePaiement.ts`) — la même contrainte CHECK a été ajoutée sur `adhesions.mode_paiement` (absente du brouillon initial) pour rester cohérente
   - Aucune PR ouverte au démarrage de cette étape
2. ✅ Restructuration navigation (accueil → dashboard, regroupement section Dons, stub section Adhérents) — PR #36 mergée le 2026-08-03
3. ✅ Formulaire + page liste + filtre + modifier/archiver — PR #37 mergée le 2026-08-03. Pagination volontairement différente de Dons/Participants (`fetchAllRows` + `.slice()` client) : ici côté serveur via RPC `search_adherents` (`LIMIT`/`OFFSET`), pour ne pas reproduire la lenteur Wat Velouvanaram (PR #32) — décision confirmée avec l'utilisateur après une question sur la cohérence entre pages
4. ✅ Import — PR #38 mergée le 2026-08-04. Système d'import déjà générique, juste une config `adherentsImportConfig` de plus. Point résolu : la contrainte d'unicité `id_externe` par organisation existait déjà (`import_id_externe_unique_constraints.sql` pour participants/activités, incluse dès l'étape 1 pour `adherents`). Décision clé : les champs de cycle (`adhesions`) ne passent jamais par l'écran de conflits du wizard, la décision "nouveau cycle ou non" est calculée côté client par comparaison au dernier cycle existant. **`date_fin` calculée automatiquement** (cycle glissant d'1 an depuis `date_debut`, `lib/adhesion.ts` `computeDateFin`) — gap découvert en session (rien ne la renseignait nulle part, le statut Actif/Expiré et la carte dashboard restaient inopérants), corrigé pour la création, le renouvellement et l'import
   - **Bugs trouvés en testant cette PR, corrigés dans la même PR :**
     - Cotisation d'import à `0` rejetée à tort (validation calquée sur celle des dons, où 0 n'a pas de sens) — corrigé, 0 est légitime pour une adhésion (organisation qui ne fait pas payer de cotisation)
     - Mode de paiement toujours validé même sans cotisation — corrigé via un nouveau hook générique `ImportConfig.postProcessRow`, ignore l'erreur sur `mode_paiement` quand `montant_cotisation` est nul/à 0
     - Mojibake UTF-8 relu en Windows-1252 sur les données source ("PHENG MÃ©lanie" au lieu de "Mélanie") — réparation automatique ajoutée dans `parseFile.ts`, générique à tous les imports (pas seulement adhérents)
5. ✅ Gabarit carte adhérent + impression planche A4 — **PR #39 mergée** (format carte de crédit ISO/IEC 7810 85,6×54mm, contenu identité+validité+logo — décisions confirmées avant codage). Nouvelle table `templates_carte_adherent` (bypass super-admin inclus dès le départ, contrairement à `templates_recu` qui avait dû être corrigé après coup), nouvelle edge function `generate-cartes-adherents` (grille 2×5 par page A4, un seul appel Gotenberg, PDF binaire direct sans persistance), sélection multiple sur `AdherentsPage.tsx` conçue de zéro (aucun pattern existant). Migrations + déploiement + requêtes de données + rendu HTML vérifiés en prod ; génération PDF réelle non testable côté agent. **Ajouté en cours de PR** (suite à des demandes de l'utilisateur) : import de modèle existant (PDF ou image) via Claude Vision, nouvelle edge function dédiée `generate-carte-adherent-template` (même principe que `generate-template-from-pdf` pour les Cerfa, pas de généralisation pour ne pas risquer la fonctionnalité Cerfa déjà en prod) ; section "Identité visuelle" (assets + président) extraite d'"Informations fiscales" vers une section indépendante, commune reçus fiscaux + carte adhérent (n'était pas spécifique aux Cerfa) ; nouveau placeholder optionnel `{{adherent_id_externe}}` (numéro externe de l'adhérent s'il existe — vide sinon, pas ajouté au gabarit visuel par défaut, juste disponible)
6. *(Prio basse, hors scope V1, reste dans le backlog actif)* Mécanisme de sélection d'un adhérent à la saisie d'un don + gestion du doublonnement

**Limitation découverte pendant l'étape 4 (non corrigée, décision utilisateur) :** les 4 fonctions RPC d'import (`import_upsert_participants`/`activites`/`dons`/`adherents`) échouent avec "Unauthorized: no organisation context" en mode super-admin "Consulter" — `current_user_organisation_id()` cherche une ligne `profils_organisation` pour l'utilisateur connecté, qu'un super-admin en consultation n'a pas. Pré-existante à ces 3 premières fonctions, jamais remarquée avant le test adhérents. Décision : se connecter en admin normal pour tester les imports plutôt que de modifier les fonctions (qui touchent des imports déjà en prod) — à reconsidérer seulement si le besoin d'importer "pour le compte d'une organisation" en tant que super-admin émerge un jour.

## Nouveau backlog (ajouté 2026-08-04) — terminé le 2026-08-07

- ✅ **Recherche adhérents par email** — PR #47 mergée. `search_adherents` étend la recherche nom/prénom existante avec un `OR courriel ILIKE` en sous-chaîne simple.
- ✅ **Incrémenter automatiquement `id_externe` à la création d'un adhérent via le formulaire** — PR #48 mergée. `next_adherent_id_externe(organisation_id)` (séquence Postgres par organisation, même pattern que `next_numero_recu`), appelée à la création uniquement (jamais en modification). Détection collision/doublon ajoutée au wizard d'import adhérents pour couvrir le risque de collision avec un futur réimport (numérotation strictement continue par organisation retenue, pas de palier haut ni de préfixe).

## Sujet "Log des modifications" + "Champ observation en cas de refus" — terminé le 2026-08-08 (PR #49 mergée)

- Table générique `journal_modifications` (append-only), périmètre de départ : création/modification/archivage/réactivation d'un adhérent, ratification/refus d'une demande d'adhésion. Diff champ par champ affiché en tooltip (hover/tap) pour les modifications d'adhérent. UI dans Paramètres (section globale + historique repliable par adhérent dans sa fiche).
- `demandes_adhesion.motif_refus` : champ texte interne saisi à la confirmation de refus, affiché dans le détail des demandes refusées — pas d'email au demandeur (flux non cadré).
- Bug post-merge trouvé et corrigé dans une PR de suivi immédiate : `CREATE OR REPLACE FUNCTION` avec des paramètres ajoutés (même avec `DEFAULT`) crée une **deuxième surcharge** plutôt que de remplacer la fonction existante côté Postgres — `list_journal_modifications` existait en double, appel ambigu, section Historique cassée en prod. `DROP FUNCTION` explicite ajouté. Bug d'affichage associé corrigé au passage (`src/lib/errors.ts`, erreurs Supabase pas `instanceof Error`).

## Intégration Trello (2026-08-07/08)

Board "Mothana" (boîte à idées de l'utilisateur) connecté en lecture/écriture via API REST directe (credentials `.env`, voir mémoire persistante). Routine actée (voir `CLAUDE.md` → Session Continuity + mémoire persistante) : check de la liste "Todo" en début de session et à chaque PR mergée signalée (cartes traitées déplacées vers "Done" automatiquement), et mise à jour de la carte (titre/description) dès qu'un sujet est cadré.

## Marathon de cadrage Trello (2026-08-08) — 10 sujets cadrés

- ✅ **CTA Ratifier/Refuser dans la modale de détail de demande d'adhésion** — PR #50 mergée. Boutons ajoutés en pied de la modale Détail (`DemandesAdhesionPage.tsx`), visibles seulement si `statut === 'en_attente'`, déclenchent les mêmes handlers que le tableau.
- ✅ **Lignes de tableau cliquables** — PR #51 mergée. Cadrée initialement sur `AdherentsPage` seul, étendue en session à la demande de l'utilisateur à tous les tableaux du projet qui s'y prêtent : `AdherentsPage` (→ modale Modifier), `DemandesAdhesionPage` (→ modale Détail), `SuperAdminPage` (→ modale Modifier l'organisation) — `ParticipantsPage`/`DonsPage` l'avaient déjà. `stopPropagation` sur les cellules interactives (case à cocher, boutons). Volontairement exclus (pas de fit naturel avec le pattern "ligne → modale unique de consultation") : `RecusFiscauxPage` (action de ligne déjà "Générer/Regénérer"), `ImportWizard` (tableau transitoire), `DeclarationCerfaCard` (synthèse agrégée). **Convention actée pour la suite** (voir `CLAUDE.md`) : appliquer ce pattern par défaut à tout nouveau tableau de ce type.
- ✅ **Validation date de naissance adhérent (année ≤ N-1)** — PR #52 mergée. Nouveau helper `src/lib/dateNaissance.ts`, appliqué à `AdherentModal` (création + modification), au formulaire public `DemandeAdhesionPage`, et au wizard d'import adhérents (`parseBirthDateCell` dans `normalizers.ts`/`fieldDefs.ts`). Garde-fou anti-erreur de saisie, pas une règle d'âge légal (loi n°2017-86 : un mineur peut librement adhérer). Bug trouvé en testant sur mobile, corrigé dans la même PR : l'attribut `max` posé sur le `<input type="date">` empêchait carrément la sélection d'une année invalide dans le picker natif au lieu de laisser choisir puis afficher l'erreur — retiré, la validation JS live (même pattern qu'email/téléphone) suffit seule. Nouvelle règle de méthode actée (mémoire persistante) : ne pas utiliser de contrainte HTML native bloquante (`max`/`min`...) quand elle peut rendre des valeurs inatteignables dans un picker mobile.
- ✅ **Débordement d'impression des cartes adhérent** — PR #53 mergée le 2026-08-09. Cause confirmée dans `generate-cartes-adherents/index.ts` : grille 2×5 (`gap: 4mm` unique) = 5×54mm + 4×4mm = 286mm, au-delà des 277mm utiles d'une page A4 (marges 10mm) → la dernière rangée débordait sur la page suivante. Fix : `gap` scindé en `row-gap: 1.4mm` (5×54 + 4×1.4 = 275.6mm) / `column-gap: 4mm` conservé (largeur largement disponible). Edge function redéployée puis testée en prod par l'utilisateur (planche multi-pages) avant ouverture de la PR — confirmé OK.
- ✅ **Message de succès personnalisable du formulaire de demande d'adhésion** — PR #54 mergée le 2026-08-09. Nouvelle colonne `organisations.formulaire_adhesion_message_succes` (text, nullable), exposée via `get_organisation_public`. Textarea simple dans Paramètres > "Adhésion en ligne" (pas de HTML riche comme l'en-tête/pied de page) ; `DemandeAdhesionPage.tsx` retombe sur le message par défaut existant si vide.
- Les 4 sujets restants du marathon (revue UI responsive admin, réorganisation Paramètres, vérification carte adhérent nom/prénom, double opt-in email, mailing Brevo) et l'OCR scan sont encore actifs — voir `CLAUDE.md` pour leur état et le lien vers leur carte Trello (source de la description complète du cadrage).

## Environnement staging Supabase — terminé le 2026-08-10

Détail complet (architecture, workflow dump/restore, commandes) : `docs/environnement-staging.md`. Résumé :

- Nouveau projet Supabase `mothana-staging` (ref `cxngcmvxktddhyxboyyx`, eu-west-1, palier gratuit). `.env` local basculé dessus ; les variables Vercel restent vers prod.
- Schéma complet répliqué via `pg_dump --schema-only` de prod (rejouer les 38 fichiers `supabase/migrations/` seuls échoue — ce dossier ne contient que les évolutions incrémentales depuis le MVP, pas un schéma de base rejouable ; `docs/schema-mothana.sql` confirmé non exécutable tel quel). `psql`/`pg_dump` locaux installés via Homebrew (`libpq`, keg-only) car `supabase db dump`/`db query -f` nécessitent Docker (absent sur cette machine) ou échouent sur les fichiers SQL multi-instructions.
- 8 Edge Functions déployées sur staging (`--use-api`, sans Docker). Secrets `ANTHROPIC_API_KEY`/`GOTENBERG_URL` configurés (rotation de `ANTHROPIC_API_KEY` faite aussi côté prod à cette occasion, l'utilisateur ayant régénéré sa clé). `APP_JWT_SECRET` confirmé mort dans le code (JWT custom déjà abandonné), pas répliqué.
- Comptes de test créés sur staging : super-admin + une organisation de démo avec templates Cerfa/carte adhérent par défaut + un compte admin.
- **Bug trouvé en seedant les données de test (pas en rapport avec le staging lui-même), corrigé au passage** : création d'une nouvelle organisation depuis `/super-admin` cassée en prod depuis le 2026-08-05 (`organisations_slug_statuts.sql` a rendu `organisations.slug` `NOT NULL` sans que `OrgModal.tsx` ne le renseigne, et sans trigger de génération automatique). Fix : `slugifyUrl` (déjà utilisé en local dans `ParametresPage.tsx` pour l'édition du slug) extrait vers `src/lib/organisationAssets.ts` (partagé), réutilisé dans `OrgModal` pour dériver le slug du nom à la création + gestion de la collision (code Postgres `23505`). Vérifié directement sur staging via le vrai flux (login super-admin + insert RLS).

## Désinscription mailing (RGPD) — terminé le 2026-08-18 (PR #65 dev)

Cadrage complet le 2026-08-17 (détail dans la session du même jour). Décisions clés : opt-out (pas opt-in bloquant), portée strictement limitée aux campagnes mailing Brevo (aucun impact sur les emails transactionnels ni sur `consent_rgpd`, notion distincte — consentement d'adhésion).

- **Migrations** : `mailing_opt_out.sql` — `adherents.mailing_opt_out`/`mailing_opt_out_at`/`mailing_unsubscribe_token` (`DEFAULT gen_random_uuid()` volatil : chaque ligne existante reçoit un token distinct à l'`ALTER TABLE`, pas de backfill manuel nécessaire). `search_adherents_mailing_opt_out.sql` en suivi immédiat (voir bug ci-dessous).
- **Edge Function `unsubscribe-mailing`** : sans auth, réponse générique anti-énumération (même pattern que `request-password-reset`), met à jour `mailing_opt_out`/`mailing_opt_out_at` par token, ignore silencieusement un token invalide.
- **`send-mailing-brevo`** : requête destinataires désormais filtrée `mailing_opt_out = false` ; footer de désinscription injecté automatiquement dans le HTML envoyé (jamais dans `corps_html` stocké en historique) avec un lien personnalisé par destinataire via le mécanisme `messageVersions`/`params` déjà utilisé pour `{{params.prenom}}` — nouveau param `lien_desinscription` construit côté serveur (`${site_url}/desinscription?token=...}`), `site_url` désormais requis en entrée (passé par le frontend via `window.location.origin`, même pattern que `request-password-reset`).
- **Page publique `/desinscription?token=...`** (`DesinscriptionMailingPage.tsx`) : appelle l'Edge Function et affiche toujours le même message de confirmation générique, y compris token invalide/absent — pas d'état d'erreur distinct exposé côté client.
- **Toggle admin "Ne pas contacter par email"** dans `AdherentModal` (création + édition). `mailing_opt_out_at` recalculé uniquement si la case a réellement changé de valeur (comparaison à un `useRef` de l'état initial) — sinon un enregistrement du formulaire sans toucher à la case aurait écrasé la date d'origine à chaque sauvegarde.
- **Bug trouvé en testant sur staging (Playwright), corrigé dans la même PR** : la page liste des adhérents passe par la RPC `search_adherents` (pas un `select('*')` direct comme `AdherentsPage` le laissait penser), qui ne renvoyait pas les nouveaux champs — la modale recevait `undefined` pour `mailing_opt_out`, la checkbox restait toujours décochée à l'ouverture quel que soit l'état réel en base (warning React "controlled/uncontrolled input" au passage, confirmant le symptôme). `CREATE OR REPLACE FUNCTION` refusant de changer la liste de colonnes en sortie d'une fonction `TABLE`, migration de suivi avec `DROP FUNCTION` explicite.
- **Validation de bout en bout sur staging**, au-delà des vérifications navigateur habituelles : envoi réel d'une campagne via Brevo à 2 adhérents fixtures jetables (adresses Yopmail complexes, créés puis supprimés après coup, données de démo pré-existantes temporairement mises en opt-out via SQL direct pendant le test puis restaurées) — confirmé via les logs d'événements Brevo (`GET /v3/smtp/statistics/events`) que le destinataire non exclu reçoit bien l'email (`delivered`) et que l'exclu n'a aucun événement (jamais sollicité), plus fiable qu'une vérification visuelle de boîte Yopmail (leur reCAPTCHA bloque l'accès automatisé — confirmé en inspectant le DOM derrière l'overlay, le badge "1 mail" affiché est un texte de gabarit statique avant chargement réel, pas une donnée fiable). L'utilisateur a ensuite testé lui-même de bout en bout (création d'un adhérent de test, envoi, réception, clic sur le lien de désinscription) et confirmé le fonctionnement, y compris la question initiale sur un lien de footer semblant pointer vers `localhost` : en réalité l'IP réseau de l'instance `npm run dev` exposée (`window.location.origin` réellement utilisé, jamais une valeur figée — reconfirmé après coup par un appel direct à l'Edge Function avec `site_url="https://test.samakan.fr"` explicite).
- Footer simplifié après la première revue utilisateur : texte d'intro retiré, lien réduit à "Se désinscrire" (au lieu de "Vous recevez cet email en tant qu'adhérent. Se désinscrire de ces communications").

## Domaine samakan.fr (apex + www) sur la prod Vercel — terminé le 2026-08-19

Cadré le 2026-08-17. Action manuelle (Vercel Settings → Domains, OVH zone DNS), pas d'accès API côté agent au départ — accès API OVH mis en place en cours de session (voir ci-dessous).

- `www.samakan.fr` connecté côté Vercel (Production, sans branche spécifique) sans souci particulier. `samakan.fr` (apex) idem.
- **Blocage rencontré** : l'ajout du CNAME `www` échouait dans le Manager OVH ("un CNAME ne peut pas coexister avec d'autres champs sur le même sous-domaine"), et la suppression des enregistrements A/TXT/AAAA affichés sous `www.samakan.fr.` dans l'interface échouait systématiquement ("Une erreur est survenue"), y compris un par un. Cause de l'échec de suppression jamais identifiée avec certitude (hypothèses explorées sans confirmation : bug de suppression groupée, DNSSEC — non vérifié faute d'accès).
- **Accès API OVH mis en place pour débloquer** : clé applicative + consumer key créées par l'utilisateur, scopées explicitement à `GET/POST/PUT/DELETE` sur `/domain/zone/samakan.fr/*` uniquement (pas d'accès au reste du compte OVH). Stockée dans `.env` (`OVH_APP_KEY`/`OVH_APP_SECRET`/`OVH_CONSUMER_KEY`), scope vérifié via `/auth/currentCredential` avant tout usage. Requêtes signées HMAC-SHA1 (algorithme standard OVH : `$1$` + sha1(AS+CK+méthode+URL+body+timestamp)), pas de SDK installé, script Python ad hoc à chaque usage.
- **Diagnostic via l'API** (invisible depuis l'interface Manager) : aucun enregistrement `www` n'existait réellement en zone — les entrées bloquantes affichées par le Manager étaient en fait rattachées à l'**apex** (`subDomain` vide), dont un `TXT` "1|www.samakan.fr" correspondant à la configuration de redirection par défaut d'OVH (parking page). Le CNAME `www` a donc pu être créé directement via l'API sans aucun conflit réel.
- **Bug plus sérieux trouvé après coup** : l'apex avait déjà un enregistrement `A` par défaut OVH (`51.91.236.255`, page de parking) jamais retiré. L'ajout de l'IP Vercel (`216.198.79.1`) a créé un **doublon** — deux `A` sur le même nom = réponse DNS en round-robin, ~50% des visiteurs auraient atterri sur la page de parking OVH au lieu du site. Même problème sur l'`AAAA` (`2001:41d0:301::29`, parking IPv6 sans équivalent IPv4 fourni par Vercel). Les deux anciens enregistrements supprimés via l'API, zone rafraîchie (`POST /domain/zone/samakan.fr/refresh`), revérifié propre sur le serveur faisant autorité (`dns106.ovh.net`) et 3 résolveurs publics indépendants (Google, Cloudflare, Quad9).
- **Redirection 308 apex→www** activée par défaut côté Vercel à l'ajout de l'apex (suggestion automatique de Vercel) — contraire à la décision actée au cadrage (les deux doivent servir directement, sans redirection). Désactivée manuellement (Project → Settings → Domains → Edit sur `samakan.fr` → "Redirect to" → None).
- Vérification finale : `curl -I` en forçant la résolution vers l'IP Vercel confirme un `200` direct sur les deux domaines, sans redirection. Cache DNS local (agent et utilisateur) resté périmé quelques minutes après les corrections — attendu, propagation déjà confirmée correcte à la source avant même l'expiration du cache local.
- Débloque "Renommage public en Samakan" (prérequis désormais levé).

## Renommage public en "Samakan" — terminé le 2026-08-19 (PR #67 dev)

Cadré le 2026-08-12 (grep exhaustif du code à l'époque, 9 endroits identifiés), travail annulé sur demande explicite ce jour-là faute du prérequis domaine, refait à l'identique une fois "Domaine samakan.fr" débloqué (voir section ci-dessus).

- **Décision** : "Mothana" reste le nom de projet interne (code, docs, projet Supabase, board Trello, dépôt Git). "Samakan" devient le nom public affiché à l'écran uniquement.
- **10 endroits modifiés** (9 du cadrage d'origine + 1 apparu depuis) : `index.html` (titre d'onglet, `mothana-temp` — reliquat de scaffolding jamais nettoyé — → `Samakan`), `HomePage.tsx`/`ResetPasswordPage.tsx`/`DesinscriptionMailingPage.tsx` (titres d'écrans publics — la 3e n'existait pas au cadrage initial, créée entre-temps par la feature RGPD, ajoutée au périmètre car même catégorie exacte), `AdminLayout.tsx`/`SuperAdminLayout.tsx` (en-têtes sidebar), `BenevolePage.tsx` (en-tête espace bénévole), `DeclarationCerfaCard.tsx` (texte de conformité "Samakan ne soumet rien automatiquement"), `ParametresOrganisationPage.tsx`/`SuperAdminPage.tsx` (placeholder d'exemple "Ex : Association Mothana" → "Ex : Les Amis du Quartier", décision actée au cadrage de ne pas laisser un exemple lié à la marque).
- **Hors scope explicite (inchangé)** : `CLAUDE.md`, `PRODUCT.md`, `DESIGN.md`, tous les `docs/*.md`, `package.json`, prompts internes des edge functions IA (jamais affichés à l'utilisateur).
- Vérifié par grep exhaustif post-modification (aucune occurrence restante dans le périmètre concerné) + vérification visuelle Playwright sur l'instance de dev (titre d'onglet, page de connexion, sidebar admin, sidebar super-admin).

## Vérification carte adhérent par nom/prénom (espace bénévole) — terminé le 2026-08-19 (PR #69 dev)

Cadré le 2026-08-08. Sujet scindé en deux au cadrage : cette carte couvre uniquement la recherche nom/prénom, le scan/photo (OCR) reste une carte séparée en backlog, pas retenue pour l'instant.

- **Nouvel onglet "Vérifier un adhérent"** dans `BenevolePage.tsx` (`/benevole`), à côté de "Saisir un don" existant — segmented control simple (state local `activeTab`), pas de nested routing. Réutilise l'auth bénévole existante (PIN → session technique `benevole-{org_id}@mothana.internal`, scope organisation déjà géré par `current_effective_organisation_id()` dans les policies RLS existantes) — pas de page publique, pas de slug.
- **RPC dédiée `search_adherents_verification`** plutôt que réutilisation telle quelle de `search_adherents` (qui renvoie toutes les colonnes de `adherents`, y compris adresse/téléphone/email) : même logique de recherche par tokens nom+prénom (AND, insensible à l'ordre, même index trigram `idx_adherents_nom_prenom`), mais colonnes de sortie strictement limitées à `nom`/`prenom`/`statut`/`date_fin` (jointure `LEFT JOIN LATERAL` sur `adhesions` pour la dernière date de fin). Décision actée au cadrage : pas la fiche complète, même pour un bénévole authentifié — la restriction est donc appliquée côté requête réseau, pas seulement côté affichage UI.
- Recherche vide = aucun résultat renvoyé (contrainte `trim(coalesce(p_search,'')) <> ''` côté SQL, défense en profondeur en plus du comportement client) : pas de listing complet des adhérents pour un bénévole, uniquement une vérification ciblée.
- Nouveau composant `src/components/BenevoleVerificationAdherent.tsx` (recherche debouncée 300ms, badge statut actif/archivé réutilisant le style déjà en place sur `AdherentsPage.tsx`, affichage "jusqu'au DATE" même pattern que la page liste adhérents).
- Testé de bout en bout sur staging via le vrai flux bénévole (connexion PIN réelle) : recherche multi-résultats (homonymes), état "aucun résultat", formulaire de don existant confirmé non cassé par l'ajout de l'onglet. **Vérification de sécurité explicite** : inspection directe de la réponse réseau brute de la RPC (`GET .../rpc/search_adherents_verification`), confirmé que seuls `nom`/`prenom`/`statut`/`date_fin` transitent — aucune fuite d'adresse/téléphone/email vers le client, conformément à la décision de cadrage.

## Sujets terminés 2026-08-11 → 2026-08-26 (extrait de `CLAUDE.md` le 2026-09-02, pour alléger le fichier chargé automatiquement à chaque session)

- **Revue UI responsive admin** (terminée 2026-08-11, PR #57) : 6 débordements mobile corrigés (table SuperAdminPage, header dupliqué dans 5 modales/pages, champ slug Paramètres), audit Playwright systématique 3 largeurs + données de test réalistes injectées dans "Association Démo Staging" (staging, conservées pour les prochains audits). Skill design QA `impeccable` (pbakaus/impeccable) installé en scope utilisateur au passage
- **Revue UI responsive admin avec le skill impeccable** (terminée 2026-08-12, PR #58) : `PRODUCT.md`/`DESIGN.md` générés (init + document), extraction `SectionHeader.tsx` (5 fichiers dupliqués), balayage exhaustif Playwright (débordement 320/390/768/1440px sur 9 pages admin + super-admin + bénévole + 20+ modales + 3 pages publiques) et axe-core WCAG AA (0 violation après correctifs contraste texte 113 occurrences/38 fichiers + labels de formulaire manquants). Détail complet sur la [carte Trello](https://trello.com/c/0cgE88pI)
- **Campagnes de mailing Brevo** (terminée 2026-08-15, PR #62) : config Brevo par organisation (clé API + expéditeur), page `/admin/adherents/mailing` (composition TipTap gras/lien/placeholders `{{params.prenom}}`/`{{params.nom}}`, pièce jointe 10 Mo max transmise en base64, filtre destinataires actif/archivé/tous, confirmation avant envoi, brouillon persistant en localStorage, historique), Edge Function `send-mailing-brevo` (mode batch natif Brevo `messageVersions`, jusqu'à 1000 destinataires/appel)
- **Tooltip stylisé sur les placeholders** (terminée, PR #59) : remplace le `title` natif par `Tooltip.tsx` (prop `bare` ajouté) sur les 3 éditeurs de template (Cerfa, carte adhérent, formulaire d'adhésion)
- **Réorganisation Paramètres** (terminée, PR #60) : 4 sous-pages routées (`/admin/parametres`, `/fiscal`, `/adherents`, `/suivi`), composant partagé `ParametresSection.tsx`
- **Environnement de recette** (terminé 2026-08-15) : `test.samakan.fr` (branche `dev`, réutilise le projet Supabase `mothana-staging`, isolation par organisation dédiée par client), nouveau workflow git `dev`→`main` pour la promotion vers prod. Détail complet : `docs/environnement-recette.md`
- **Email d'invitation admin + mot de passe oublié via Resend** (terminé 2026-08-15, PR #63 dev, PR #64 promotion prod) : `create-admin` génère un lien d'invitation (`auth.admin.generateLink`) au lieu d'un mot de passe en clair, nouvelle Edge Function `request-password-reset` (réponse générique anti-énumération), les deux envoyées via Resend (template HTML Mothana). Un seul compte Resend pour dev/recette/prod (domaine `samakan.fr` vérifié). Validé de bout en bout sur la recette avant promotion
- **Page de tour produit `/decouvrir`** (terminé 2026-08-21, PR #71 dev, PR #72 promotion prod) : tour produit pour clients/prospects, 5 sections (Dons, Adhérents, Mailing, Espace bénévole, Personnalisation), sommaire à ancres, captures réelles encadrées façon navigateur. Initialement nommée `/documentation`, renommée après relecture utilisateur (galerie/accroches marketing ≠ vraie doc instructionnelle — scindé en carte séparée "Vraie documentation utilisateur", pas encore cadrée). Captures prises sur l'organisation "Association Démo Staging" (staging) après purge complète des données réelles et régénération d'un jeu 100% fictif. Bug de confidentialité trouvé et corrigé en cours de route : la config Brevo de l'organisation démo exposait une vraie adresse email personnelle de l'utilisateur, remplacée par une valeur fictive avant capture.
- **Corriger le branding Mothana→Samakan dans les emails** (terminé 2026-08-21, PR #73 dev, PR #74 promotion prod) : `create-admin`/`request-password-reset` (titre, sujet, expéditeur). Amélioration ajoutée en testant sur Yopmail : l'email d'invitation admin précise désormais le nom de l'organisation concernée (récupéré côté serveur, pas transmis par le frontend). En testant cette PR, un blocage plus large a aussi été détecté et cadré séparément (créer un admin avec un email déjà utilisé par une autre organisation) : voir "Support multi-organisation pour un compte admin" dans le backlog actif de `CLAUDE.md`.
- **Bandeau visuel distinguant l'environnement recette de la prod** (terminé 2026-08-21, PR #75 + #76 dev, PR #77 promotion prod) : bannière ambre "Environnement de test", détection par `window.location.hostname`. Périmètre initial (`AdminLayout` + `BenevolePage`) étendu en PR #76 après un gap trouvé par l'utilisateur en testant sur la vraie recette : ajouté aussi sur `SuperAdminLayout` et la mire de connexion (`HomePage`).
- **Identifier l'espace bénévole + l'organisation dans le header** (terminé 2026-08-22, PR #78 dev, PR #83 promotion prod) : header `/benevole` sur 2 lignes, ligne 1 inchangée ("Samakan" + Quitter), ligne 2 "Espace bénévole — *Organisation*" (même pattern de fetch que `AdminLayout.tsx`). Vérifié de bout en bout sur la recette via le vrai flux PIN bénévole. Promotion faite par cherry-pick du commit de code sur une branche dédiée (pas un `dev → main` direct) pour garder les promotions séparées malgré les 4 autres features déjà mergées sur `dev` depuis — les commits `docs:` associés restent sur `dev` uniquement, sans conséquence (`CLAUDE.md` n'est pas exécutable).
- **Afficher le numéro d'adhérent dans la vérification bénévole** (terminé 2026-08-22, PR #79 dev, PR #84 promotion prod) : `search_adherents_verification` renvoie désormais `id_externe` (numéro d'adhérent, pas une donnée sensible), affiché "N° adhérent : …" dans `BenevoleVerificationAdherent.tsx`, masqué si vide. Vérifié de bout en bout sur la recette via le vrai flux PIN bénévole. Promotion par cherry-pick (même méthode que PR #83) + migration `search_adherents_verification_id_externe.sql` rejouée sur prod, fonction vérifiée présente après coup.
- **Corriger le calcul de hauteur mobile (100vh → dvh)** (terminé 2026-08-22, PR #80 dev, PR #85 promotion prod) : `min-h-screen`/`h-screen` remplacés par les utilitaires `dvh` de Tailwind sur 11 fichiers/14 occurrences (`w-screen` non touché, pas concerné par le problème). Blocage trouvé par l'utilisateur en testant sur un vrai téléphone (barre d'adresse Android Chrome) : la modale par défaut de `Modal.tsx` (`max-h-[90vh]`, hors périmètre du grep initial car syntaxe Tailwind arbitraire) avait le même symptôme — haut inaccessible au scroll, combiné au centrage flex du composant. Corrigé dans la même PR (`max-h-[90dvh]`), composant partagé donc toutes les modales de l'app en bénéficient. Promotion par cherry-pick (même méthode que PR #83/#84), pas de migration.
- **Garde-fou de suppression d'organisation** (terminé 2026-08-22, PR #81 dev, PR #86 promotion prod) : comptage `adherents` ajouté (à côté de donateurs/dons déjà calculés), modale de suppression affiche les 3 chiffres, confirmation façon GitHub (saisie du nom exact pour activer le bouton). Étendu en cours de route sur demandes de l'utilisateur : CTA "Modifier" retiré de la ligne (déjà cliquable, doublon), "Supprimer" déplacé dans le pied de la modale d'édition ; modale "Admins" fusionnée dans "Modifier l'organisation" (CTA "Admins" retiré) ; CTA "Consulter" remplacé par un picto œil ; toast de confirmation après suppression réussie (réutilise `Toast`/`useToast` existant). Vérifié de bout en bout sur staging (super-admin réel), y compris via une organisation jetable créée puis supprimée pour ne pas toucher aux données de test existantes. Promotion par cherry-pick (même méthode que PR #83/#84/#85), pas de migration.
- **Nettoyer la dette lint** (terminé 2026-08-22, PR #82 dev, PR #87 promotion prod) : 34 → 0 problème. Le gros morceau (28× `react-hooks/set-state-in-effect`) s'est révélé être un faux positif documenté de la règle sur le pattern fetch-au-montage (recommandé par la doc React elle-même, cf. [react/react#34743](https://github.com/react/react/issues/34743)) — écarté du cadrage initial : désactivé dans `eslint.config.js` plutôt que contourné par ~28 hacks `setTimeout`. Les 6 restants traités au cas par cas (pas d'ajout aveugle de dépendance) : `useAuth` extrait dans `src/hooks/useAuth.ts` + `AuthContext`/`AuthContextValue`/`AuthState` dans `src/contexts/authContextInstance.ts` (Fast Refresh), `fetchActivites`/`loadData` passées en `useCallback`, 2 assignations mortes retirées, 1 dépendance volontairement exclue (documentée en commentaire). Vérifié : eslint 0 problème, typecheck, build prod, et les 3 flux d'auth (admin/super-admin/bénévole) sur staging. Promotion par cherry-pick (même méthode que PR #83-86), pas de migration — dernière des 5 promotions du 2026-08-22, `dev` et `main` de nouveau alignés côté code (seuls les commits `docs:` restent propres à `dev`).
- **Envoi du reçu fiscal par email au donateur** (terminé 2026-08-22, PR #90 dev, PR #91 promotion prod) : bouton "Email" à côté de "PDF" dans `RecusFiscauxPage.tsx` (masqué si le participant n'a pas d'adresse email), nouvelle Edge Function `send-recu-email` (télécharge le PDF depuis le bucket privé `recus-fiscaux`, l'attache en base64 via Resend, pose `recus_fiscaux.email_envoye_at` — colonne déjà en base depuis la refonte Cerfa, jamais utilisée jusqu'ici, affichée "Envoyé le …" dans le tableau). Portée individuelle, déclenchement manuel, un envoi à la fois. Limitation super-admin confirmée en testant, erreur réseau ponctuelle sur mobile/VPN de l'utilisateur non reproduite (probablement liée à l'environnement local de test, pas un bug de l'app). Déployée et testée de bout en bout sur staging avant merge. Promotion par cherry-pick, pas de migration (colonne déjà présente en prod, vérifié directement en base) — Edge Function déployée explicitement sur prod après le merge de promotion.
- **Toast de confirmation après ajout d'un compte admin** (terminé 2026-08-22, PR #92 dev, PR #94 promotion prod) : nouveau prop `onAdminAdded` sur `OrgModal` (même pattern que `onSaved`/`onDeleteRequest`), réutilise le `Toast`/`useToast` déjà en place dans `SuperAdminPage.tsx`. Vérifié de bout en bout sur staging (super-admin réel, compte de test créé puis désactivé). Promotion par cherry-pick, pas de migration.
- **Reçus fiscaux : champ de recherche par nom** (terminé 2026-08-23, PR #93 dev, PR #95 promotion prod) : réutilise le helper existant `matchesParticipantSearch` (`src/lib/participantSearch.ts`), même emplacement/style que le champ de recherche d'`ActivitesPage.tsx`. Filtrage client-side, pagination et compteur mis à jour sur le résultat filtré, état "Aucun résultat" distinct de "Aucun don enregistré". Vérifié visuellement (résultat, sans résultat, champ masqué si aucun don sur l'année). Promotion par cherry-pick, pas de migration.
- **Listes de diffusion (tags adhérents)** (terminé 2026-08-25, PR #96 dev, PR #97 promotion prod) : colonne `adherents.tags text[]` (appartenance) + table `listes_diffusion` (registre des noms de liste par organisation, alimentée automatiquement par trigger dès qu'un tag est utilisé) — pivot d'architecture décidé en cours de dev sur retour utilisateur, pour permettre de créer une liste vide sans adhérent présélectionné (le cadrage initial prévoyait "pas de table dédiée", explicitement abandonné). Affectation en masse depuis `AdherentsPage.tsx` (bouton "Nouvelle liste", RPC batch `add_adherents_tag`), nuage de tags éditable dans `AdherentModal.tsx` (`TagsInput.tsx` partagé), filtres liste + exclusion regroupés visuellement, sélecteur "Envoyer à" unifié statut/liste sur `CampagneMailingPage.tsx` (une liste prime sur le statut), `send-mailing-brevo` étendu. Bug de build Vercel trouvé et corrigé en cours de route (prop `availableTags` rendue obligatoire à tort, cassait `DemandesAdhesionPage.tsx` qui réutilise aussi `AdherentModal`) — détecté par le déploiement, pas par `tsc --noEmit` local (config racine sans project references sans `-b`, désormais `tsc -b` utilisé pour matcher la commande Vercel). Vérifié de bout en bout sur la recette (Playwright) à 2 reprises (avant et après le pivot table). Promotion par cherry-pick des 7 commits de la PR #96 (une seule migration en 2 fichiers séquentiels, ordre respecté : `adherents_tags.sql` puis `listes_diffusion.sql`) + déploiement explicite de `send-mailing-brevo` sur prod — dump prod pris avant migration (`~/dump_prod_avant_listes_diffusion_20260825.sql`).
- **Configuration Brevo : CTA + modale de configuration** (terminé 2026-08-25, PR #98 dev, PR #99 promotion prod) : formulaire (clé API, expéditeur) sorti de `CampagneMailingPage.tsx` vers `BrevoConfigModal.tsx`, indicateur "Configuré"/"Non configuré" + bouton "Configurer" sur la page, bouton "Enregistrer" affiché seulement en état "dirty" (au moins un champ différent des valeurs chargées). Validation live du format email de l'expéditeur ajoutée en cours de revue (même pattern `isValidEmail` que `AdherentModal.tsx`, absente du cadrage initial). Vérifié sur staging (Playwright). Promotion par cherry-pick des 2 commits de la PR #98, pas de migration ni Edge Function concernée.
- **Mailing : joindre plusieurs fichiers (max 3)** (terminé 2026-08-25, PR #100 dev, PR #102 dev, PR #103 promotion prod) : `send-mailing-brevo` envoyait déjà un tableau d'attachments à Brevo, seul le frontend limitait à 1 fichier. `pieceJointe` → `piecesJointes[]`, payload `piece_jointe` → `pieces_jointes`, plafond 10 Mo par fichier conservé, input masqué une fois le max atteint. Un blocage Brevo rencontré en testant (expéditeur `contact@samakan.fr` non validé, "Sending has been rejected") — pas un bug Mothana, résolu côté compte Brevo par l'utilisateur (authentification du domaine). Retour utilisateur après ce test : le picker natif n'acceptait qu'un fichier à la fois (Android + Chrome macOS) faute d'attribut `multiple` sur l'`<input type="file">` — corrigé en PR #102 (`handleAttachmentsChange` traite désormais toute la `FileList`). Testé par l'utilisateur, fonctionnel. Promu en prod avec le fix id_externe (PR #103, batch explicitement demandé par l'utilisateur).
- **Fix : collision `id_externe` adhérent après import** (terminé 2026-08-25, PR #101 dev, PR #103 promotion prod, pas de carte Trello — bug ad hoc trouvé par l'utilisateur) : `next_adherent_id_externe()` ne resynchronisait jamais sa séquence sur le vrai max après un import (`import_upsert_adherents` ne touche jamais la séquence), provoquant `duplicate key value violates unique constraint adherents_id_externe_unique`. Fix : `GREATEST(last_value, max réel)` recalculé à chaque appel. Reproduit et vérifié sur staging (séquence à 13 vs max réel 16, puis création réelle réussie). Promotion par cherry-pick des 3 commits (PR #100/#101/#102) en un seul batch sur demande explicite ("Tu peux promouvoir les PR"), migration `next_adherent_id_externe_resync.sql` rejouée sur prod (vérifiée présente) + `send-mailing-brevo` redéployée — dump prod pris avant (`~/dump_prod_avant_mailing_id_externe_fix_20260825.sql`).
- **Fix : compteur de destinataires figé après rechargement (mailing)** (terminé 2026-08-26, PR #104 dev, PR #105 promotion prod) : condition de course entre deux fetches de `fetchDestinatairesCount` au chargement de la page (1 avec la valeur par défaut de `envoyerA`, 1 avec la vraie valeur restaurée du brouillon localStorage) — sans garde, le fetch qui répond en dernier écrase l'autre. Fix : compteur de requête incrémental, seul le résultat du dernier appel lancé est appliqué. Portée confirmée en creusant le bug : l'envoi réel (`handleSend`) recalcule toujours la cible depuis l'état `envoyerA` au clic, jamais depuis ce compteur — aucun risque d'avoir envoyé à la mauvaise liste, seul l'aperçu affiché pouvait être trompeur. Reproduit et vérifié (5/5 tentatives correctes après le fix). Incident DNS sans rapport découvert et corrigé au passage : l'authentification du domaine `samakan.fr` dans Brevo (faite par l'utilisateur pour la validation d'expéditeur) avait ajouté un CNAME sur `test.samakan.fr` pour le tracking de liens/images, écrasant le CNAME existant vers Vercel — recette cassée jusqu'à correction du DNS (voir mémoire `reference_ovh_api.md`). Promotion par cherry-pick, pas de migration ni Edge Function concernée.

## Sujets terminés 2026-08-26 → 2026-09-01 (extrait de `CLAUDE.md` le 2026-09-02, même raison qu'au-dessus)

- **Peuplement automatique d'une organisation de test (staging)** (terminé 2026-08-26, PR #106 dev, PR #110 promotion prod) : la création d'une organisation via le super-admin, sur staging/recette uniquement (double garde-fou hostname + projet Supabase lié = `mothana-staging`, `src/lib/environment.ts`), insère automatiquement un jeu de données factice fixe (2 activités, 3 donateurs, 5 dons, 4 adhérents à statuts variés actif/archivé) juste après les templates par défaut. Bug RLS trouvé et corrigé au passage : la policy `activites_write_admin` n'avait pas le bypass `is_super_admin` déjà présent sur `dons`/`adherents`/`profils_participant`/`personnes`, bloquant toute écriture d'activité par un super-admin — migration `activites_super_admin_bypass.sql`, rejouée sur prod (sans effet pratique en prod, le peuplement lui-même reste gardé staging-only, mais corrige un vrai bug RLS présent aussi en prod). Vérifié de bout en bout (Playwright + vérification base + contrôle visuel des pages Dons/Adhérents/Activités).
- **Cerfa : détail des dons de l'année** (terminé 2026-08-27, PR #108 dev, PR #110 promotion prod) : nouveau placeholder `{{dons_detail}}` dans `generate-recu` (tableau Événement/Montant/Date/Mode, à intégrer manuellement dans les templates, pas de backfill). Regroupement "Nb Mois" uniquement sur `dons.don_regulier_id` (2 dons ou plus) — pas sur `mode_paiement=3` seul, écarté après analyse des vraies données de prod (99,5% des dons en mode 3 sont uniques dans l'année, indiscernables entre virement ponctuel et engagement historique saisi en une fois). CSS par défaut aligné sur le reste du template (classes `dons-detail`/`dons-detail-total`, nouvelles organisations uniquement). Deux bugs trouvés par l'utilisateur en testant sur son propre template avant merge, corrigés dans la même PR : (1) le placeholder manquait dans la liste de l'éditeur (`cerfaPreview.ts`, simple oubli) ; (2) la colonne Événement restait vide car basée sur `activites.id_externe` (rempli seulement pour les activités importées) — remplacé par `activites.nom`, qui fonctionne dans tous les cas. Amélioration ajoutée au passage : modifier l'activité d'un engagement "Dons réguliers" propage désormais le changement aux dons déjà générés (le montant reste volontairement futurs-uniquement, pour ne pas désynchroniser un reçu fiscal déjà émis).
- **Adhérents mobile : CTA "Sélectionner"** (terminé 2026-08-27, PR #109 dev, PR #110 promotion prod) : bouton "Sélectionner" (mobile uniquement, `sm:hidden`) togglant l'affichage de la colonne checkbox d'`AdherentsPage.tsx` (sélection multiple pour impression de cartes, coûteuse en largeur sur mobile) ; desktop inchangé. Désactiver le mode vide la sélection en cours. Vérifié visuellement 390px/1440px sur staging.
- **Dons réguliers : automatiser les prélèvements mensuels récurrents** (terminé 2026-08-26, PR #107 dev, PR #110 promotion prod) : nouvelle page `/admin/dons-reguliers` (entrée sidebar sous "Dons") pour gérer des engagements de don récurrent (participant, montant, jour de prélèvement, activité optionnelle, dates début/fin) avec une section "Dons à confirmer" listant tous les mois manquants depuis la date de début de chaque engagement actif — jamais d'insertion automatique sans confirmation admin (contrainte fiscale Cerfa). Nouvelle table `dons_reguliers` + colonne `dons.don_regulier_id` (RLS avec bypass super-admin inclus dès le départ, migration rejouée sur prod). Ajouté en cours de route sur demande de l'utilisateur : bandeau "Dons réguliers en attente de confirmation" sur le tableau de bord (`DashboardPage.tsx`), même pattern que le bandeau existant des demandes d'adhésion. Vérifié de bout en bout sur staging (backfill 4 mois, confirmation partielle, Arrêter un engagement).

**Promotion PR #110 (2026-08-27)** : `dev`/`main` réalignés en une seule PR (`gh pr create`/`gh pr merge --merge`, `dev` conservée) plutôt qu'un cherry-pick par sujet — les 4 PR dev (#106/#107/#108/#109) sont montées telles quelles, aucune divergence à gérer. Dump prod pris avant migration (`~/dump_prod_avant_promotion_20260827.sql`, supprimé après succès), migrations `activites_super_admin_bypass.sql` puis `dons_reguliers.sql` rejouées et vérifiées présentes sur prod, `generate-recu` redéployée sur prod. CLI Supabase relié à `mothana-staging` après coup (lien temporairement basculé sur prod le temps des migrations).

- **Animation d'ouverture des sidepanels mobile (Dons + Participants)** (terminé 2026-08-30, PR #111 dev) : le panneau de détail mobile ("slide over") de `DonsPage.tsx` et `ParticipantsPage.tsx` apparaissait instantanément, sans transition. Fix : réutilise exactement le pattern déjà en place dans `Toast.tsx` — state `mobilePanelVisible` + `useEffect` déclenchant `setTimeout(10ms)` au montage du panneau (uniquement quand la sélection passe de `null` à une valeur, pas en changeant de sélection) + classes `translate-x-full`/`translate-x-0` + `transition-transform duration-200`. Pas d'animation de sortie, comme cadré. Vérifié via Playwright (390px, staging) sur les deux pages.
- **Pilote shadcn/ui — reconstruction visuelle ActivitesPage** (terminé 2026-08-31, PR #112 dev) : parti d'une reformulation en cours de session de "Audit mobile colonnes" — l'utilisateur a exprimé le souhait de migrer toute l'app vers shadcn/ui (mobile-first, plus soigné). Analyse faisabilité/coût faite en chat (aucune fondation shadcn existante, 24 pages, dette non factorée documentée dans `DESIGN.md`) → décision : pilote isolé sur une seule page avant tout rollout, plutôt qu'un big-bang. Fondation posée (`src/lib/utils.ts`, `src/components/ui/{button,dialog,input,label}.tsx`, tokens namespacés `paper`/`ink`/`stamp` + polices `registre`/`registre-mono` dans `tailwind.config.cjs` — namespacés pour ne pas impacter les 23 pages non migrées). `ActivitesPage.tsx` entièrement reconstruite, direction "carnet tamponné x registre" choisie et affinée avec l'utilisateur sur plusieurs itérations (skill impeccable — tirage de direction, puis 3 corrections utilisateur : cachet redéfini comme fait dérivé des dates réelles plutôt qu'un statut de workflow inventé après vérification du vrai schéma `Activite`, police mono restreinte aux données tabulaires seulement, sections actives/à venir vs terminées séparées). Compteurs réels dons/participants par activité ajoutés au passage (nouveauté factuelle, absente avant ce chantier). Fonctionnalité 100% préservée, testée de bout en bout sur staging (Playwright, cycle CRUD complet). Revue de finition du skill impeccable (2 tours : contraste AA, anneau de focus, canvas papier, hiérarchie du cachet par statut — tous résolus). `DESIGN.md` racine non modifié (reste la référence pour les pages non migrées) — nouvelle direction tracée dans une fiche de surface dédiée (`.impeccable/surfaces/`). Bug corrigé au passage (trouvé en testant, sans rapport direct) : `tailwind.config.js` en ESM empêchait le serveur de dev **permanent** de recharger les nouveaux tokens sans redémarrage (jamais fait) — renommé en `.cjs`. **Décision prise le 2026-08-31** : généralisation validée par l'utilisateur ("on peut les attaquer 1 par 1, avec une PR à chaque fois") — voir carte Trello "Généraliser shadcn/ui aux 23 pages restantes" (Todo, suivi de la progression page par page).
- **Rollout shadcn/ui — DonsReguliersPage** (terminé 2026-08-31, PR #113 dev, 2e page du rollout, 2/24) : même direction "carnet tamponné x registre" et mêmes primitives que le pilote, aucun nouveau composant nécessaire. Différence assumée : le statut actif/arrêté d'un engagement est une vraie action manuelle de l'admin (bouton Arrêter/Réactiver), rendu en badge texte sobre plutôt qu'en cachet — le cachet reste réservé aux faits purement dérivés de dates, pour ne pas diluer son sens. Fonctionnalité 100% préservée (dons à confirmer avec montants éditables, CRUD engagement, toggle statut), testée de bout en bout sur staging. Seam assumé hors scope : `ParticipantAutocomplete`/`ActiviteAutocomplete` (composants partagés, pas encore migrés) restent dans l'ancien système visuel à l'intérieur des nouvelles modales.
- **Rollout shadcn/ui — DemandesAdhesionPage** (terminé 2026-08-31, PR #114 dev, 3e page du rollout, 3/24) : 1ère page à vrai `<table>` — nouvelle primitive `Table` (`src/components/ui/table.tsx`) + `Textarea`, réutilisables pour les 5 autres pages à tableau restantes. Nouveaux tokens sémantiques `warning` (ambre, doublon détecté) et `success` (vert registre, ratification) dans `tailwind.config.cjs`, distincts de l'encre de marque `stamp` — même principe que l'ancien `DESIGN.md` (couleurs réservées à la signalisation d'état, jamais décoratives), contraste AA vérifié par calcul (5,0:1 et 6,2:1 sur blanc). Fonctionnalité 100% préservée (onglets, détection de doublons, détail, refus avec motif, ratification via `AdherentModal` inchangé). Bug corrigé dans la même PR (trouvé en testant) : la modale de détail débordait horizontalement à cause de Téléphone/Courriel en colonnes 50% — passés en pleine largeur (Civilité/Date de naissance restent groupées, formats courts fixes) + `break-words` en défense. Seam assumé hors scope : `AdherentModal` (593 lignes, partagé) reste dans l'ancien système visuel.
- **Rollout shadcn/ui — AdherentsPage** (terminé 2026-09-01, PR #115 dev, 5e page du rollout, 5/24) : nouvelles primitives `Select` (natif réhabillé, pas un popover custom — mieux adapté à l'audience du projet) et `Badge`. Deux natures de statut distinguées sur la même ligne : actif/archivé (classement manuel) en badge neutre, cycle d'adhésion (actif/expiré/aucune, un vrai fait dérivé de dates) en badge `success`/`warning`. Deux bugs réels trouvés par l'utilisateur en testant, corrigés dans la même PR :
  - Débordement horizontal sur mobile étroit (~360px, capture d'un vrai téléphone) : `flex-shrink-0` sur un groupe de boutons empêchait flex-wrap de se déclencher, un bouton se retrouvait rendu hors du viewport et inatteignable (`AdminLayout` a `overflow-hidden` sur son conteneur racine, pas de scroll de secours). Corrigé par `w-full sm:w-auto`. **Leçon retenue pour les pages suivantes du rollout : tester systématiquement à 320/360/390px, pas seulement 390px.**
  - CTA de ligne sans hiérarchie sémantique : 2 itérations (`ghost` → `secondary` → nouveau variant Button `danger`, texte stamp dès le repos) pour restaurer la distinction neutre/marque/positif/négatif que l'ancien design avait déjà. Corrigé sur les 4 pages du rollout d'un coup (primitive partagée). `destructive` (plein) reste réservé aux boutons de confirmation finale en modale.
  - Dette identifiée mais non traitée dans cette PR (décision explicite) : `AdherentModal` (593 lignes, partagé avec `DemandesAdhesionPage`) reste dans l'ancien système — sujet remis à une PR dédiée après la prochaine page du rollout.
- **AdherentModal migré vers shadcn** (terminé 2026-09-01, PR #116 dev) : dette de la PR #115 traitée en PR dédiée (composant partagé, met à jour AdherentsPage et DemandesAdhesionPage d'un coup). Primitive `Dialog` adaptée pour supporter un formulaire long — `flex flex-col overflow-hidden` plutôt que `grid ... overflow-y-auto`, header et pied de page restent fixes, seul le corps scrolle ; retrofit de sécurité appliqué aux dialogues déjà existants (Activites/DonsReguliers/DemandesAdhesion/Adherents), sans changement visuel dans les cas normaux. Fonctionnalité 100% préservée (création, édition avec historique, ratification avec bannière de doublons). Testé de bout en bout, donnée de test créée via le vrai formulaire puis nettoyée en base (CLI Supabase, ligne exacte ciblée par id). Seams restants (composants partagés avec des pages non migrées) : `TagsInput`, `AdherentHistoriqueSection`, `AssignerListeModal`, `AdhesionModal`. **Point de vigilance noté par l'utilisateur, décision explicite de différer** : le contenu des formulaires (labels, regroupement des champs, cases à cocher, en-têtes de section) n'a pas eu de vraie passe de finition — migration fonctionnelle correcte mais esthétique perçue comme "non aboutie" comparée aux listes/tableaux. À traiter en une passe groupée sur tous les formulaires migrés, une fois plus de pages/modales traitées.
- **Rollout shadcn/ui — DonsPage** (terminé 2026-09-01, PR #117 dev, 6e page du rollout, 6/24) : filtres (raccourcis de période, dates, participant/activité/mode) + stats + tableau + panneau de détail (desktop en panneau latéral, mobile en overlay glissant). Décision de design : les 4 modes de paiement gardent un badge `neutral` unique plutôt que 4 couleurs décoratives (vert/ambre/bleu/gris dans l'ancien design) — pas un vrai signal d'état, aurait dilué la réserve sémantique déjà actée sur `warning`/`success`. Animation du panneau de détail mobile (slide-over, PR #111) préservée à l'identique, seule la palette change. Leçons des pages précédentes appliquées dès le départ (boutons en `flex-wrap`, tests systématiques 320/360/390px) — un seul petit débordement de pagination à 320px trouvé et corrigé. Fonctionnalité 100% préservée, testée de bout en bout sur staging. Seams assumés hors scope : `DonModal`, `ParticipantAutocomplete`, `ActiviteAutocomplete`, `ImportWizard`.
- **DonModal migré vers shadcn** (terminé 2026-09-02, PR #119 dev) : dette identifiée en PR #118 (composant partagé par DonsPage/DonsReguliersPage/ParticipantsPage, les 3 déjà migrées — même situation qu'AdherentModal avant sa PR dédiée). Migration fonctionnelle vers `Dialog`/`DialogContent`/`DialogHeader`/`DialogTitle` (même pattern qu'`AdherentModal`), `Input`/`Label`/`Select`/`Button`. `ParticipantAutocomplete`, `ActiviteAutocomplete`, `ParticipantModal` restent des seams hors scope. **3 bugs d'intégration Dialog-sur-Modal trouvés et corrigés en testant le flux imbriqué "+ Nouveau participant"** (ouvre l'ancien `ParticipantModal` depuis le nouveau `DonModal`) :
  - Position:fixed cassé : `ParticipantModal` rendu à l'intérieur de `DialogContent` héritait du containing block créé par le `translate` CSS de ce dernier (la modale imbriquée se retrouvait positionnée/dimensionnée relativement à la boîte du Dialog parent, pas au viewport). Fix : rendu hors de `DialogContent`, en sibling du `Dialog`.
  - Clics bloqués une fois sorti du `Dialog` : `ParticipantModal` héritait `pointer-events: none` de `<body>` (posé par Radix tant qu'un Dialog est ouvert, seul son propre Content étant explicitement réautorisé). Fix : nouvelle prop `Modal.tsx` `elevated` — porte le modal via `createPortal` vers `document.body` (même niveau que le Portal Radix) + `pointer-events-auto` explicite sur le wrapper.
  - Fermeture en cascade : Radix interprétait tout clic/Escape dans le modal imbriqué comme "à l'extérieur" du Dialog parent (celui-ci n'étant plus un descendant DOM de `DialogContent` après le fix précédent) et le fermait aussi — repéré en testant : cliquer "Fermer" sur le participant fermait aussi le don en cours de saisie. Une garde basée sur l'état React (`fullModalOpen`) s'est révélée insuffisante (l'état avait déjà changé au moment où l'event Radix se déclenche, condition de course). Fix : `onInteractOutside`/`onEscapeKeyDown` du `DialogContent` parent ignorés quand la cible de l'événement provient du modal imbriqué, détecté via un marqueur DOM stable (`data-elevated-modal`) plutôt que l'état.
  - Fonctionnalité 100% préservée, vérifiée de bout en bout sur staging (Playwright) : formulaire d'ajout (DonsPage), flux imbriqué complet (ouverture, saisie, fermeture sans fermer le don), 390px, et ouverture depuis `ParticipantsPage` (panneau de détail → "Ajouter un don").
- **Rollout shadcn/ui — CampagneMailingPage** (terminé 2026-09-03, PR #120 dev, 8e page du rollout, 8/24) : 1ère page de la famille "Paramètres" (config Brevo, composition d'une campagne, historique). `BrevoConfigModal` migré dans la même PR (composant privé à cette page, pas un seam partagé, contrairement à AdherentModal/DonModal). Décision de scope : `ParametresSection` (composant partagé avec les 4 autres sous-pages Paramètres, aucune encore migrée) volontairement non touchée — remplacée localement par une carte propre à la page (`SectionCard`, non exportée) pour ne pas changer le rendu visuel des pages non migrées qui l'utilisent encore. Nouveau seam identifié : `ParametresSection`. Bug trouvé en migrant et corrigé dans la même PR : le message d'erreur d'envoi restait invisible tant que l'ancienne modale de confirmation (overlay plein écran) était ouverte — déplacé dans la modale elle-même. Fonctionnalité 100% préservée (éditeur riche Tiptap, placeholders `{{params.prenom/nom}}`, pièces jointes, sélecteur de liste de diffusion, brouillon localStorage), vérifiée de bout en bout sur staging (Playwright) à 320/360/390/768/1440px — aucun débordement, aucune écriture réelle en base pendant les tests (envoi annulé avant confirmation).
- **Fix : focus à l'ouverture des modales** (terminé 2026-09-03, même PR #120, demande explicite de l'utilisateur en relisant la PR) : Radix focalise par défaut le premier élément focusable d'une `DialogContent` à l'ouverture — souvent un champ de formulaire, jugé intrusif. Changé dans la primitive partagée `ui/dialog.tsx` (`onOpenAutoFocus` déplacé vers le bouton Fermer) : s'applique rétroactivement à toutes les modales déjà migrées (DonModal, AdherentModal, confirmations de suppression, etc.), pas seulement celles de cette PR. Vérifié sur la modale Brevo, la confirmation d'envoi et DonModal (non touché par cette PR, juste consommateur de la primitive) — focus sur Fermer partout, aucune régression.
- **Rollout shadcn/ui — RecusFiscauxPage** (terminé 2026-09-03, PR #121 dev, 9e page du rollout, 9/24) : mêmes primitives que les pages à tableau précédentes (`Button`/`Input`/`Select`/`Badge`/`Table`/`Dialog`), aucun nouveau composant. Bannière "organisation incomplète" en `warning` (remplace l'ambre indigo), statuts Généré/Non généré en `Badge` `success`/`neutral`, confirmation de régénération migrée de l'ancien `Modal` vers `Dialog` (bénéficie au passage du fix de focus de la PR #120). Seam `ParticipantModal` (non migré) laissé tel quel, testé sans conflit visuel/z-index avec la nouvelle page (contrairement au cas DonModal/ParticipantModal de la PR #119, pas de nesting Dialog-dans-Dialog ici). Fonctionnalité 100% préservée (génération/régénération de reçu, téléchargement PDF, envoi par email, recherche, pagination, sélecteur d'année), vérifiée de bout en bout sur staging (Playwright) à 320/360/390/768px, `ScrollShadowX` gère le scroll horizontal du tableau sur mobile.
- **Rollout shadcn/ui — SuperAdminPage** (terminé 2026-09-03, PR #122 dev, 10e page du rollout, 10/24) : `OrgModal` (création/édition d'organisation + gestion des comptes admin) migré dans la même PR — composant privé à cette page, pas un seam. Première utilisation du variant `Button` `ghost`/`icon` (bouton "Consulter", icône œil) et du badge `stamp` (statut "Désactivé" d'un compte admin), deux motifs déjà présents dans les primitives partagées mais pas encore consommés ailleurs. Confirmation de suppression d'organisation (saisie du nom exact, style GitHub) migrée de l'ancien `Modal` vers `Dialog`. Fonctionnalité 100% préservée (création/édition, ajout/désactivation/réactivation de comptes admin, suppression avec confirmation, navigation "Consulter" vers une organisation), vérifiée de bout en bout sur staging (Playwright, super-admin réel) à 320/360/390/768/1440px. Point de vigilance noté par l'utilisateur en relisant la PR, ajouté sur la carte Trello du rollout (pas encore traité) : passe groupée future sur les CTA à icône seule quand la correspondance iconographie/action est évidente (ex. "Consulter"), à faire une fois plus de pages migrées — même logique que la passe de finition des formulaires déjà différée.
- **Rollout shadcn/ui — ParticipantsPage** (terminé 2026-09-02, PR #118 dev, 7e page du rollout, 7/24) : même famille que DonsPage (recherche, tri de colonnes, tableau, pagination, panneau de détail desktop latéral/mobile overlay). Variation assumée : le panneau plafonne dynamiquement sa hauteur (ref + `useLayoutEffect`, repris tel quel de l'ancien code) pour absorber un historique de dons de longueur variable — contrairement au panneau fixe de DonsPage. `ScrollShadowX` réappliqué sur le tableau (convention AdherentsPage/DemandesAdhesionPage — DonsPage ne l'a pas, oubli probable de cette PR-là, non corrigé, hors scope de celle-ci). Fonctionnalité 100% préservée (recherche, tri, historique des dons avec badge neutre unique par mode de paiement, suppression avec confirmation en Dialog), vérifiée de bout en bout sur staging (Playwright) à 320/360/390/768/1440px — aucun débordement horizontal. Seams hors scope : `ParticipantModal`, `DonModal` (ce dernier traité juste après, PR #119).
- **Rollout shadcn/ui — ParametresSuiviPage + déblocage ParametresSection** (terminé 2026-09-03, PR #123 dev, 11e page du rollout, 11/24) : première page de la famille "Paramètres" à profiter du seam `ParametresSection` débloqué — migrée vers les nouveaux tokens `paper`/`ink`/`stamp` directement (plutôt que dupliquer un `SectionCard` local comme fait pour `CampagneMailingPage`, qui n'avait pas cet intérêt). Conséquence assumée et voulue : les 3 sous-pages Paramètres pas encore migrées (Organisation, Fiscalité, Adhérents) affichent désormais une carte au nouveau style (coins moins arrondis, sans ombre) avec un contenu resté dans l'ancien système — écart visuel mineur et temporaire, jusqu'à leur propre migration (prochaines de la liste). `HistoriqueModificationsSection.tsx`/`HistoriqueModificationsModal.tsx` migrés dans la même PR (privés à cette page, pas des seams). `JournalActionLabel.tsx` volontairement non touché — seam partagé avec `AdherentHistoriqueSection.tsx` (encore non migré, utilisé dans `AdherentModal.tsx` déjà migré), impact visuel jugé minime (couleurs de texte proches, pas de dissonance de mise en page). Fonctionnalité 100% préservée (liste des 10 dernières modifications, tooltips de détail, modale "Voir plus" paginée), vérifiée de bout en bout sur staging (Playwright) à 320/360/390/768/1440px, y compris l'impact visuel sur les 3 sous-pages non migrées.
- **Rollout shadcn/ui — ParametresFiscalPage + templates de reçus** (terminé 2026-09-03, PR #124 dev, 12e page du rollout, 12/24) : 2e page de la famille "Paramètres" à bénéficier du seam `ParametresSection` débloqué par la PR #123. Toute la famille de composants migrée dans la même PR : `TemplatesRecuSection` (liste des templates 11580/16216, badges de statut, confirmations archiver/supprimer), `TemplateRecuPreviewModal` et `TemplateRecuImportPdfModal` (composants privés à cette page, pas des seams), et `TemplateRecuEditorModal` — le plus gros morceau du rollout à ce jour (éditeur Monaco HTML/CSS avec onglets, aperçu iframe live, popover placeholders obligatoires/optionnels avec copie presse-papier). **Nouveau : prop `fullScreen` ajoutée à la primitive partagée `ui/dialog.tsx`** (`DialogContent`) pour permettre à un Dialog Radix d'occuper tout l'écran (toggle utilisateur, pas un choix fixe à l'ouverture) — l'éditeur de template en avait besoin et aucune modale migrée jusqu'ici n'avait ce cas d'usage ; généralisable aux futures migrations de `CarteAdherentEditorModal` et `FormulaireAdhesionEditorModal`, qui ont le même besoin de plein écran. Bug trouvé et corrigé en testant à 320px (leçon des pages précédentes appliquée systématiquement) : le bouton "Nouveau template" était tronqué en bord d'écran — `shrink-0` sur le conteneur des deux boutons d'action l'empêchait de se laisser contraindre par la ligne parente, empêchant son propre `flex-wrap` interne de faire passer le 2e bouton à la ligne ; retiré. Fonctionnalité 100% préservée (formulaire fiscal, CRUD templates, activation/archivage/suppression avec vérification d'usage, import PDF, édition avec Ctrl+S), vérifiée de bout en bout sur staging (Playwright, admin démo) à 320/360/390/768/1440px, aucune erreur console. `tsc -b` et `eslint` propres.
- **Rollout shadcn/ui — ParametresAdherentsPage + carte adhérent** (terminé 2026-09-04, PR #125 dev, 13e page du rollout, 13/24) : 3e et dernière page du groupe Paramètres avant `ParametresOrganisationPage`. Toute la famille de composants migrée dans la même PR : `CarteAdherentSection` (quasi-clone structurel de `TemplatesRecuSection` de la PR #124 — même pattern liste/badges/confirmations, fix `shrink-0` réappliqué directement sans redécouverte), `CarteAdherentPreviewModal`, `CarteAdherentImportModal`, `CarteAdherentEditorModal` (quasi-clone de `TemplateRecuEditorModal`, sans le sélecteur de type Cerfa, réutilise la prop `fullScreen` de la PR #124), et `FormulaireAdhesionEditorModal` (variante 3 onglets En-tête/Pied de page/CSS, pas de badge obligatoire/optionnel sur les placeholders, bouton "Restaurer les valeurs par défaut" en plus). Fonctionnalité 100% préservée (slug public, upload statuts PDF, CRUD gabarits de carte, éditeur en-tête/pied de page du formulaire d'adhésion), vérifiée de bout en bout sur staging (Playwright, admin démo) à 320/360/390/768/1440px, aucune erreur console. `tsc -b` et `eslint` propres.
- **Fix : aperçu Cerfa/carte coupé sans scroll sur mobile** (terminé 2026-09-04, même PR #125) : bug remonté par l'utilisateur en testant sur téléphone (capture d'écran) — l'aperçu d'un template de reçu (`TemplateRecuPreviewModal`, PR #124 déjà mergée) coupait le contenu (signature, mentions finales) sans possibilité de scroller, sur un viewport mobile court (barre d'adresse visible). Cause : le corps de la modale utilisait `overflow-hidden` au lieu d'`overflow-y-auto`. Corrigé dans `TemplateRecuPreviewModal.tsx` et, en prévention, dans `CarteAdherentPreviewModal.tsx` (même pattern copié dans cette PR, même bug latent) : `flex-1 overflow-y-auto` sur le corps, `shrink-0` sur l'en-tête (même convention qu'`AdherentModal`), hauteur de l'iframe passée de `vh` à `dvh` pour rester cohérente avec le `max-h-[90dvh]` de la coquille `DialogContent` — l'écart entre les deux unités sur un viewport mobile avec barre d'adresse visible (`vh` reste calé sur le plus grand viewport possible, `dvh` suit le viewport réellement visible) est la cause racine du débordement non-scrollable. Vérifié par Playwright (viewport réduit, scroll programmatique confirmant le contenu complet désormais atteignable).
- **Rollout shadcn/ui — ParametresOrganisationPage** (terminé 2026-09-04, PR #126 dev, 14e page du rollout, 14/24) : 4e et dernière page du groupe Paramètres — le groupe est désormais entièrement migré (ParametresSuiviPage #123, ParametresFiscalPage #124, ParametresAdherentsPage #125, ParametresOrganisationPage #126). Page autonome, aucun seam ni modale partagée : informations générales (nom de l'association), code PIN bénévole (afficher/masquer via toggle œil, régénérer via Edge Function `update-pin`), identité visuelle (nom/titre du président + grille d'assets — logo/tampon/signature — upload/remplacement/suppression, chaque asset devient un placeholder `{{asset_<identifiant>}}`). Aucun nouveau composant, primitives `Button`/`Input`/`Label` déjà existantes. Fonctionnalité 100% préservée, vérifiée de bout en bout sur staging (Playwright, admin démo) à 320/360/390/768/1440px, aucune erreur console. `tsc -b` et `eslint` propres.
- **Rollout shadcn/ui — les 10 dernières pages, en lot** (terminé 2026-09-04, PR #127 à #137 dev) : l'utilisateur a demandé de traiter le reste du rollout d'un coup — une branche + une PR par page depuis `dev` (jamais un lot de commits dans une seule PR), puis une revue/merge page par page ensuite, avec bascule de branche à la demande. Chaque page a suivi la même méthode que le reste du rollout (tokens `paper`/`ink`/`stamp`/`warning`/`success`, tests systématiques 320/360/390/768/1440px, `tsc -b`/`eslint` propres avant push) :
  - **DashboardPage** (PR #127, 15e page, 15/24) : 1er écran après connexion. Nouveau pattern de bannière d'action (cercle icône + CTA pill), structure `flex-col`/`sm:flex-row` responsive. Bug corrigé en testant à 320px : le bloc texte s'écrasait au lieu de laisser le CTA passer à la ligne.
  - **SuperAdminLayout** (PR #128, 16e page, 16/24) : chrome uniquement (badge "Super Admin" `stamp`, bouton déconnexion `secondary`), `<main>` inchangé.
  - **ComptabilitePage** (PR #129, 17e page, 17/24) : chrome de page + `DeclarationCerfaCard` (tableau natif → `Table`). Constantes couleur Recharts (`CATEGORICAL`/`ACCENT`/`CONTEXT_GRAY`/etc.) volontairement non touchées, palette dataviz validée séparément.
  - **HomePage** (PR #130, 18e page, 18/24) : mire de connexion admin, 1ère impression de l'appli.
  - **BenevoleLoginPage** (PR #131, 19e page, 19/24) : accent rose consolidé en `stamp`. Champ PIN garde volontairement `text-3xl`/`tracking-[0.5em]` (exception assumée), passé en `font-registre-mono`.
  - **ResetPasswordPage** (PR #132, 20e page, 20/24) : page 4 états (lien invalide / formulaire / mots de passe différents / succès), route réelle `/mot-de-passe/nouveau`. État "succès" vérifié par lecture de code plutôt qu'en direct pour ne pas changer le mot de passe du compte démo staging partagé (l'état "invalide" de cette page reflète en réalité l'absence de session admin active, pas un token d'URL — donc testable sans vrai lien de récupération).
  - **DemandeAdhesionPage** (PR #133, 21e page, 21/24) : formulaire public d'adhésion (`/adhesion/:slug`). `SignaturePad` migré dans la même PR (trait du stylo `#1e293b` → `#241f19`). `ShadowHtmlBlock` (bloc HTML personnalisable par organisation, isolé en shadow DOM) volontairement non touché.
  - **DesinscriptionMailingPage** (PR #134, 22e page, 22/24) : page triviale de confirmation (`/desinscription`).
  - **DecouvrirPage** (PR #135, 23e page, 23/24) : page vitrine prospects. Hero fond sombre `bg-ink`/`text-paper` avec CTA plein `bg-stamp text-white` (exception assumée au bouton outline par défaut, contraste sur fond sombre) ; CTA du footer (fond clair) en bouton standard outline. Limitation connue et acceptée : 15 captures d'écran embarquées montrent encore l'ancienne UI — nouvelle carte Trello créée pour ce sujet (pas encore cadrée).
  - **BenevolePage** (PR #136, 24e page, 24/24) : plus grosse page du lot (639 lignes), interface terrain bénévole. `ActiviteAutocomplete` migré dans la même PR — dernier seam de composant partagé débloqué. `BenevoleVerificationAdherent` migré (badges Actif/Archivé). `PinOverlay` ré-habillé mais reste un overlay custom (pas un Dialog Radix).
  - **AdminLayout** (PR #137, 25e et dernière PR) : sidebar/header partagé par toutes les pages admin (`bg-ink`/lien actif `bg-stamp`), overlay mobile, top bar, bannière super-admin. `RecetteBanner` migré dans la même PR — dernier seam débloqué. Padding `p-6` du `<main>` conservé à l'identique (convention dont dépendent toutes les pages migrées).

  **Rollout terminé : 24/24 pages migrées, tous les seams débloqués.** Erreur de suivi corrigée en route : la page `ParametresSuiviPage` (PR #123) avait été omise de la liste numérotée de la carte Trello de suivi depuis sa création — corrigé rétroactivement. Carte Trello "Généraliser shadcn/ui aux 23 pages restantes" déplacée en Done. `DESIGN.md` racine reste non réécrit (sujet séparé, pas un prérequis à la clôture de ce chantier). Passes différées toujours en attente (non traitées dans ce lot, à regrouper plus tard) : finition esthétique du contenu des formulaires en modale, passe CTA à icône seule.

## Batch "Saisie d'un don" — 2 cartes chaînées (2026-09-07/08)

Premier batch exécuté selon la nouvelle méthode "batch dev" (1 go pour tout le batch, PR chaînées carte par carte sans attendre confirmation ni merge entre les deux) : "Batch — Saisie d'un don" (liste Trello dédiée, archivée une fois les 2 cartes en Done).

- **Pièces jointes sur un don** (terminé 2026-09-07, PR #139 dev) : upload/consultation de fichiers (images/PDF, 10 Mo max) sur un don, admin (`DonModal.tsx`) et bénévole (`BenevolePage.tsx`). Nouvelle table `dons_fichiers` + bucket Storage privé `dons-fichiers` (URL signées à la demande, même logique que `recus-fiscaux`). Conception finale en plusieurs itérations suite à des retours utilisateur en testant :
  - Le champ apparaissait d'abord seulement **après** un premier "Enregistrer" (contrainte `don_id` non nul) — jugé confus par l'utilisateur. Refondu en mode "staged" : `DonFichiers.tsx` accepte `donId: string | null`, les fichiers choisis avant l'existence du don sont mis en attente côté client (upload différé) puis envoyés d'un coup via une méthode impérative (`forwardRef`/`uploadStaged`) juste après la création du don — un seul clic "Enregistrer" suffit, plus de bouton "Terminer" séparé.
  - Miniatures cliquables ajoutées ensuite (image réelle via URL signée ou `URL.createObjectURL` pour les fichiers en attente, icône générique pour les PDF) + lightbox (`Dialog`) pour agrandir une image, et la section pièces jointes affichée aussi directement dans le panneau de détail d'un don (`DonsPage.tsx`), pas seulement en édition.
  - **Bug bloquant trouvé en testant, sans rapport direct avec les pièces jointes** : le formulaire bénévole envoyait `mode_paiement` en texte (`'virement'/'cheque'/'especes'`) alors que la colonne est un `smallint` depuis une migration antérieure côté admin — **aucun don bénévole ne pouvait être enregistré**, corrigé dans la même PR (remplacé par le même `ModePaiement`/`MODE_PAIEMENT_OPTIONS` que `DonModal.tsx`).
  - **2e bug bloquant trouvé en testant** : le compte technique bénévole peut avoir été créé avant l'existence de la logique `app_metadata` (role/organisation_id) — `verify-pin` ne la mettait à jour qu'à la création du compte, jamais ensuite. Edge function corrigée pour se corriger elle-même (self-heal) à chaque connexion, redéployée sur staging.
  - **Bug RLS trouvé en usage réel par l'utilisateur (pas en test automatisé)** : en mode consultation super-admin, l'upload échouait systématiquement ("new row violates row-level security policy") alors que le don lui-même s'enregistrait. Cause : `dons_fichiers_insert` (table) et `dons_fichiers_storage_insert` (bucket) n'avaient pas le bypass `is_super_admin` déjà présent sur les policies select/delete sœurs — oubli, pas une limitation voulue (contrairement aux limitations déjà actées sur les imports/reçus fiscaux). Corrigé et vérifié en conditions réelles (login super-admin → Consulter → upload). **Réflexe retenu pour tous les prochains dev touchant des tables organisation-scoped : le bypass super-admin doit être posé sur les 4 opérations CRUD dès la première migration, l'utilisateur testant habituellement dans ce mode.**
  - Toast de confirmation ajouté (réutilise `useToast`/`Toast` déjà en place ailleurs) : remplace le message d'erreur transitoire dans la modale, coupé net si l'utilisateur clique en dehors pour essayer de le lire.
  - Bonus UI demandé en cours de route sur `DonsPage.tsx` : bloc Stats déplacé avant le bloc Filtres (désormais replié par défaut dans un expandable).

- **Sélection d'un adhérent à la saisie d'un don + gestion du doublonnement** (terminé 2026-09-07, PR #140 dev, empilée sur #139) : si aucun participant existant ne correspond au nom saisi, recherche de repli côté adhérents (`search_adherents` admin fiche complète, `search_adherents_verification` bénévole champs restreints nom/prénom/statut/date_fin). Suggestion distincte des résultats participants, vérification des détails, confirmation explicite obligatoire avant de transposer l'adhérent en nouveau participant (`personnes`+`profils_participant`, ids générés côté client) — sans lien permanent stocké (pas de colonne `adherent_id`), la transposition ponctuelle suffit à éviter les doublons futurs.
  - **Bug trouvé en testant, corrigé dans la même PR** : la création du donateur transposé avait lieu immédiatement au clic "Confirmer", avant même la validation du don — un don annulé après confirmation laissait un donateur fantôme en base. Corrigé en différant l'écriture réelle (personnes/profils_participant) jusqu'à la validation effective du don, avec les ids déjà réservés côté client pour l'UI. Complication trouvée au passage : l'état "en attente" ne peut pas vivre dans `AdherentFallbackSuggestions` lui-même (il se démonte dès qu'un participant est sélectionné) — remonté dans `DonModal`/`BenevolePage`, qui survivent tout le cycle de vie de la saisie.

Vérifié de bout en bout sur les deux PR (Playwright, admin + bénévole + super-admin Consulter, staging), `tsc -b` propre, données de test nettoyées en base et storage après chaque vérification.

- **Switches d'activation de fonctionnalités par organisation** (terminé 2026-09-08, PR #142 dev, batch "Super-admin : cycle de vie organisation") : colonne jsonb `fonctionnalites_activees` sur `organisations` (`{"dons": true, "adherents": true}` par défaut, extensible sans migration à chaque feature ajoutée). Checkboxes Dons/Adhérents dans la modale d'édition d'organisation (super-admin). Blocage UI + garde de route (`FeatureGuard`, pas de RLS — contrôle d'affichage/navigation, pas une frontière de sécurité) sur nav admin (groupe Dons, groupe Adhérents, Comptabilité qui dépend de Dons, Paramètres > Adhérents), onglets bénévole (bascule auto sur l'onglet actif restant, message dédié si les deux désactivés) et widgets dashboard accueil liés.
  - **Suite en cours** (non mergée) : bouton "Enregistrer" de `OrgModal` désormais désactivé tant que le formulaire n'a pas changé + toast de confirmation cohérent avec le reste de la page — trouvé en testant ce même flux, corrigé dans la PR #143 (archivage, empilée) plutôt que de rouvrir une PR sur la branche déjà mergée.

- **Suppression d'organisation : archivage intermédiaire** (terminé 2026-09-08, PR #143 dev, empilée sur #142, batch "Super-admin : cycle de vie organisation" — liste Trello clôturée) : colonne `archived_at timestamptz` sur `organisations` (réversible). Bouton "Supprimer" de la modale d'édition devient "Archiver" pour une organisation active. Connexion bloquée pour les admins/bénévoles d'une organisation archivée (`loginAdmin` + Edge Function `verify-pin`) — applicatif, pas de RLS. Nouvel onglet "Archivées" dans le dashboard super-admin : extraction CSV par table (adhérents/dons/participants/activités), réactivation, suppression définitive (réutilise la modale de confirmation nominative PR #81/#86).
  - **Bug trouvé en testant, corrigé dans la même PR** : race condition dans `AuthContext` entre `loginAdmin` et le listener `onAuthStateChange` — les deux réagissaient à l'événement `SIGNED_IN` et pouvaient chacun déclencher un `signOut()`, produisant parfois le mauvais message d'erreur au blocage de connexion. Fixé avec un flag `suppressListenerRef` pendant que `loginAdmin` pilote explicitement la résolution.
  - **2e bug trouvé en testant, corrigé dans la même PR** : la suppression définitive d'une organisation cascade `profils_organisation` mais pas les comptes `auth.users` associés (cascade FK à sens unique) — les comptes admin restaient orphelins, incapables de se connecter et impossibles à gérer depuis l'UI. Nouvelle Edge Function `delete-org-admins` (service role) appelée avant la suppression de l'organisation ; la modale de confirmation nominative affiche désormais aussi le nombre de comptes admin concernés.
  - **Incident en testant (hors PR, leçon retenue)** : une organisation de test préexistante ("Wat Velouvanaram Test", utilisée par de vrais testeurs) a été supprimée définitivement pendant la validation du flux Archiver → Supprimer, sans confirmation préalable — perte irréversible confirmée (`supabase backups list` : pas de PITR ni de sauvegarde physique sur le projet staging). Organisation recréée à vide et les 2 comptes admin orphelins rattachés manuellement (`profils_organisation`) ; seules les données métier de test (2 adhérents) sont définitivement perdues. Nouvelle règle retenue (mémoire persistante) : ne plus jamais tester un flux destructif que sur des données créées par l'agent lui-même dans la session.
  - Bouton "Enregistrer" de `OrgModal` désactivé tant que le formulaire n'a pas changé + toast de confirmation cohérent avec le reste de la page (repéré en testant les switches, PR #142, corrigé ici).

- **Audit mobile : masquer les colonnes redondantes avec la vue détail (Dons, Participants)** (terminé 2026-09-09, PR #145 dev, batch "Batch 1") : `hidden md:table-cell` sur Activité/Mode (DonsPage.tsx) et Civilité (ParticipantsPage.tsx) — déjà visibles dans le `DetailPanel` respectif de chaque page, redondantes sur mobile où un clic ouvre déjà le détail. Ajout d'un passthrough `className` sur `SortableHead` (ParticipantsPage) pour permettre ce masquage sans casser le tri.
  - **Retours de QA appliqués dans la même PR** (liste Trello dédiée "Pr 145", cf. mémoire persistante — pratique généralisée à toutes les futures PR) : pièces jointes du side panel de détail d'un don passées en lecture seule (nouveau prop `canAdd` sur `DonFichiers`, `false` uniquement dans `DonsPage.tsx` — l'ajout reste dans la modale de modification) ; colonne "Total dons" également masquée sous `md` sur `ParticipantsPage.tsx` (déjà visible dans son `DetailPanel`).

- **Super-admin : simplifier les tableaux d'organisations + revoir l'en-tête mobile** (terminé 2026-09-09, PR #146 dev, batch "Batch 1" — liste Trello clôturée) : retrait des 3 cartes stats globales du dashboard (compte d'organisations actives déplacé dans le libellé de l'onglet "Actives (N)", même modèle que "Archivées (N)"). Table "Actives" réduite à 1 colonne (nom + PIN), sans en-tête — date de création et bouton "Consulter" déplacés dans l'en-tête de la modale de détails. Table "Archivées" allégée (retrait Participants/Total collecté). En-tête du tableau (titre + onglets) : stack vertical + onglets pleine largeur sous `sm`.
  - **Retours de QA appliqués dans la même PR** (listes Trello dédiées "Pr 145"/"Pr 146" — pratique désormais généralisée à toutes les futures PR, cf. mémoire persistante) :
    - Bouton "Consulter" recalé (`-mt-2`) pour aligner son centre vertical avec le bouton de fermeture de la modale (mesuré via bounding box Playwright).
    - Table "Archivées" : retrait complet du thead, texte "Archivée le JJ/MM/AAAA" affiché directement dans la cellule (remplace l'affichage du PIN, inutile pour une organisation archivée).
    - "Comptes admin" de la modale : les comptes désactivés sont masqués par défaut, avec un lien "Afficher les comptes désactivés (N)" pour les révéler.
    - Lignes d'organisations archivées rendues lisibles sur mobile (`TableRow`/`TableCell` en `block` sous `md`, actions en `flex-wrap`) — débordaient hors écran auparavant (scroll horizontal nécessaire).
    - Nettoyage hors-sujet demandé explicitement dans la même PR : tailles de police en dur (`text-[11px]`/`text-[10px]`, dette préexistante repérée par le hook de design impeccable) remontées au palier documenté `text-xs` dans `ParticipantsPage.tsx` (DetailPanel) et `DonFichiers.tsx`.
  - **Conflit de rebase rencontré et résolu** : la PR avait été rebasée sur `dev` avant que la PR #145 (qui touche aussi `DonFichiers.tsx`/`ParticipantsPage.tsx`) soit mergée ; les retouches ultérieures de nettoyage typographique sur ces mêmes fichiers ont ensuite divergé de la version mergée de #145 sur `DonFichiers.tsx` (le nouveau prop `canAdd` de #145 vs. le `text-xs` de #146 sur la même ligne). Résolu par un rebase sur `dev` à jour, fusion manuelle conservant les deux changements (wrapper `canAdd` + `text-xs`), force-push — `mergeable: MERGEABLE` confirmé après coup.

## Batch "Campagnes mailing" (2026-09-09/10)

Go donné pour tout le batch en une fois (liste Trello "Batch — Campagnes mailing", 2 cartes, archivée à la clôture). Contrairement au batch précédent, aucune des deux cartes ne dépend fonctionnellement de l'autre — chaque branche repart de `dev` directement (pas empilée), conformément à la règle par défaut.

- **Modèles réutilisables pour les campagnes mailing** (terminé 2026-09-09, PR #147 dev) : table `mailing_templates` (organisation_id, nom, sujet, corps_html), RLS complète avec bypass super-admin sur les 4 opérations dès la première migration. UI dans `CampagneMailingPage` : bouton "Modèles (N)" ouvrant une modale de gestion — cliquer sur un nom charge le modèle dans le composeur et ferme la modale (même geste pour l'utiliser ou le modifier : réenregistrer sous le même nom déclenche l'écrasement, avec avertissement), renommer/supprimer possibles depuis la même modale.
  - **Itération de design en cours de route, hors bug** : la première version utilisait un `<select>` "Charger un modèle" à côté du bouton "Gérer les modèles". Remonté par l'utilisateur en testant : un select est un état persistant, or le brouillon localStorage restauré au chargement de la page pouvait laisser du contenu dans le composeur alors que le select affichait toujours "Partir d'un message vide" — confusion entre les deux mécanismes de persistance (brouillon localStorage = filet de sécurité contre une perte accidentelle, modèle = contenu réutilisable délibéré, décidé de garder les deux). Remplacé par le bouton "Modèles" ouvrant la modale de gestion, chaque ligne cliquable pour charger — plus d'état persistant trompeur, une seule action à chaque clic.
  - **2 bugs mobiles trouvés en testant, corrigés dans la même PR** : (1) la rangée "Envoyer la campagne"/"Enregistrer comme modèle" (`flex` sans `flex-wrap`) forçait toute la page à déborder horizontalement sous la largeur d'un écran de téléphone — `flex-wrap` ajouté. (2) le bouton "Modèles" en `variant="ghost"` (pas de bordure/fond au repos) se lisait comme du texte simple plutôt qu'un bouton — passé en `variant="secondary"`, cohérent avec "Enregistrer comme modèle" juste en dessous. **Réflexe retenu** (mémoire persistante) : vérifier systématiquement un viewport mobile (375px) sur toute modif UI, pas seulement quand la tâche est explicitement "mobile" — ce deuxième bug n'aurait pas existé si le premier avait été détecté avant la fin de la carte plutôt qu'après une remontée utilisateur.

- **Détection d'emails adhérents invalides (bounces)** (terminé 2026-09-10, PR #148 dev) : abandon du double opt-in actif (cadrage initial) au profit d'une détection passive. Confirmation de réception à la soumission d'une demande d'adhésion → trigger DB (`pg_net`, `supabase_functions.http_request` indisponible sur ce projet car fonctionnalité Database Webhooks jamais activée côté dashboard) → edge function `send-demande-confirmation` → email Resend taggé `demande_id`. Bounce Resend → `resend-bounce-webhook` (signature Svix vérifiée à la main via Web Crypto) → `demandes_adhesion.email_bounced_at`. Bounce Brevo (campagnes mailing) → `brevo-bounce-webhook` → `adherents.email_invalide_at`, webhook auto-enregistré côté Brevo par `register-brevo-webhook` à chaque sauvegarde de la clé API (idempotent, corrige aussi les événements d'un webhook déjà existant). Copié à la ratification, remis à `null` si le courriel est corrigé. Helper Resend partagé (`_shared/resend.ts`), `create-admin`/`request-password-reset` migrés dessus. Badge "Email invalide" (DemandesAdhesionPage, AdherentModal). Dashboard : bannière d'action ouvrant une modale nommant les adhérents concernés, clic direct vers leur fiche en édition (demande utilisateur en testant, remplace un premier compteur chiffré jugé pas assez visible).
  - **5 bugs trouvés en testant, corrigés dans la même PR** : (1) RPC `search_adherents` — signature réellement en place différente de celle supposée (une migration ultérieure avait ajouté `p_tag`/`p_exclude_tag`), la première tentative créait un overload fantôme au lieu de remplacer la bonne, laissant `email_invalide_at` jamais transmis à `AdherentModal` malgré la colonne posée en base. (2) `register-brevo-webhook` : l'API Brevo renvoie `document_not_found` (pas une liste vide) quand le compte n'a encore aucun webhook — traité comme erreur au départ. (3) Nom d'événement Brevo réel pour un domaine sans DNS/MX : ni `invalid_email` (cadrage initial) ni `blocked` (1ère hypothèse), mais `error` — confirmé via le journal Transactionnel Brevo après deux hypothèses infructueuses (webhook à 0 tentatives à chaque fois, faute de matcher le bon événement). (4) `brevo-bounce-webhook`/`resend-bounce-webhook` déployées sans `--no-verify-jwt` au départ — la vérification JWT par défaut bloquait tout appel entrant (services externes, pas d'utilisateur Supabase authentifié). (5) Interface "Charger un modèle" — sans rapport direct, cf. carte précédente, corrigé au passage sur la même page.
  - **Limitation levée le 2026-09-11** (pendant la session de cadrage business plan, remontée hors-sujet par l'utilisateur qui a observé un vrai bounce Resend après ~14h de "Delivery Delayed") : un vrai bounce a bien fini par arriver côté Resend, mais `email_bounced_at` n'était pas mis à jour — **6e bug**, propre à ce volet, trouvé en conditions réelles. Cause : `resend-bounce-webhook` supposait `data.tags` au format tableau `{name, value}[]` (celui utilisé à l'*envoi* de l'email via `sendViaResend`), alors que le *webhook* de retour Resend renvoie `tags` comme un objet clé/valeur (`{"demande_id": "..."}`) — `tags.find(...)` levait une `TypeError`, capturée par le handler générique et renvoyée en 500 (visible dans le journal de tentatives Resend, `resend.com/webhooks/...`, la même leçon que pour Brevo : vérifier le journal de livraison réel plutôt que de deviner). Corrigé (`tags` typé `Record<string, string>`, lecture par clé) et redéployé. Redéploiement initial sans `--no-verify-jwt` (régression du bug (4) ci-dessus, réintroduite par un simple `supabase functions deploy` sans l'option) — recorrigé immédiatement. Confirmé fonctionnel de bout en bout après un "Replay" de l'événement côté Resend : `email_bounced_at` posé en base et badge "Email invalide" affiché dans `DemandesAdhesionPage`. Carte Trello mise à jour.

- **Campagne courrier : planches d'étiquettes adhérents (14/A4)** (terminé 2026-09-11, PR #150 dev) : nouvelle page `/admin/campagne-courrier`, Edge Function `generate-campagne-courrier` (PDF A4, planches de 14 étiquettes) + table `campagnes_courrier` (historique minimal : activité, sélection, nombre de destinataires/exclus, écrite uniquement côté service role). Nom du PDF dérivé de l'activité et de la date.

**Promotion PR #151-156 (2026-09-11)** : 6 promotions `dev` → `main` en une session (#145 audit mobile, #146 super-admin tableaux, #147 modèles mailing, #148 détection bounces, #149 fix tags resend, #150 campagne courrier) — `dev`/`main` déjà alignés côté code au moment de démarrer, chaque promotion en PR séparée (pas de lot). Backend prod appliqué après coup, dans une session distincte (le merge GitHub ne déclenche jamais migrations/déploiement sur ce projet) :
  - **Blocage rencontré puis levé** : la connexion directe à la base prod (`psql`/`pg_dump` vers `db.bocqfdhmxmleracrwvbu`) a d'abord été bloquée par le classifier de sécurité de Claude Code, action jugée sensible sous le mode auto/autonome alors actif — comportement attendu du classifier, pas un bug. Débloqué après désactivation du mode auto par l'utilisateur : la même connexion passe alors normalement (avec confirmation utilisateur au lieu d'un blocage systématique).
  - #145/#146 : front only, rien à appliquer en prod.
  - #147/#148/#150 : dump prod pris avant chaque lot (`pg_dump` via Homebrew, Docker absent sur la machine), migrations rejouées dans l'ordre de dépendance (`mailing_templates.sql`, `bounce_detection_adherents.sql`, `demande_adhesion_confirmation_webhook.sql`, `search_adherents_email_invalide.sql`, `campagnes_courrier.sql`), toutes vérifiées présentes après coup. `demande_adhesion_confirmation_webhook.sql` contient en dur l'URL + clé publishable du projet **staging** (documenté dans le fichier) : substitution vers les valeurs prod faite dans une copie temporaire hors dépôt avant exécution, fichier source resté inchangé (sert tel quel pour `dev`/staging).
  - Edge Functions déployées explicitement sur prod : `generate-campagne-courrier`, `send-demande-confirmation`, `resend-bounce-webhook` (`--no-verify-jwt`), `brevo-bounce-webhook` (`--no-verify-jwt`), `register-brevo-webhook`, `create-admin` et `request-password-reset` (redéployées pour le helper Resend partagé) — réglages `verify_jwt` vérifiés identiques au staging après déploiement.
  - CLI relinkée sur `mothana-staging` à la fin (état par défaut du projet), dumps prod temporaires supprimés après vérification du succès.
  - **À faire par l'utilisateur (pas une action CLI)** : pour toute organisation prod utilisant déjà Brevo, resauvegarder la clé API dans la modale de configuration une fois pour déclencher l'enregistrement du webhook `brevo-bounce-webhook` côté Brevo (`register-brevo-webhook` s'exécute à la sauvegarde, pas rétroactivement).

- **Données de démo staging : noms classiques** (terminé 2026-09-11, PR #157 dev) : `src/lib/demoOrgSeed.ts` mélangeait noms lao et français sans raison fonctionnelle — remplacés par des noms français classiques (Bernard, Petit, Lefebvre, Moreau…), même structure/montants/statuts/tags. Injection ad hoc du même jeu de données (×1 pour les 2 activités, ×50 pour adhérents/participants/dons, données variées) dans l'organisation de test "Wat Velouvanaram Test" (staging), recréée vide après l'incident de suppression du 2026-09-08 — pas de carte Trello, demande directe hors backlog.

- **Page Adhérents : filtres email, code postal, ville, pays** (terminé 2026-09-11, PR #158 dev) : 4 nouveaux filtres dans la barre existante — select "Email" à 3 états (Tous/Avec email/Sans email, même pattern que le filtre Statut), 3 champs texte "contient" (code postal, ville, pays). RPC `search_adherents` étendue de 4 paramètres optionnels (signature réellement en place revérifiée avant le `DROP FUNCTION`, point d'attention connu sur cette RPC). Vérifié sur staging (org "Wat Velouvanaram Test") desktop + mobile (375px).

**Promotion PR #159 (2026-09-11)** : `dev`/`main` réalignés en une seule PR directe (base `main`, head `dev`, `gh pr merge --merge --delete-branch=false`) plutôt qu'un cherry-pick par sujet — aucune divergence entre les deux branches au moment de la promotion (PR #157 + #158 montées telles quelles), même méthode que PR #144. Dump prod pris avant migration, `search_adherents_filtres_email_localisation.sql` rejouée et vérifiée sur prod (PR #157 n'a pas de migration, `demoOrgSeed.ts` est staging-only). CLI relinkée sur `mothana-staging` à la fin, dump temporaire supprimé.

- **Bug prod : lien reset password (et autres liens auth) pointant vers `mothana.vercel.app`** (terminé 2026-09-12, PR #160 dev, mergée) : diagnostic initial erroné — d'abord attribué à `window.location.origin` envoyé tel quel par le front (`HomePage.tsx`, `SuperAdminPage.tsx`, `CampagneMailingPage.tsx`) comme `site_url` vers les Edge Functions `request-password-reset`/`create-admin`/`send-mailing-brevo`. L'utilisateur a fait remarquer avoir déclenché la demande depuis `samakan.fr` et pas `mothana.vercel.app`, ce qui invalidait cette explication. **Cause réelle** trouvée en comparant la config Auth (Management API `GET /v1/projects/{ref}/config/auth`) des projets prod et staging : le projet prod (`bocqfdhmxmleracrwvbu`) avait encore `site_url = https://mothana.vercel.app` et une `uri_allow_list` ne contenant que `mothana.vercel.app/**` et `localhost:3000/**` — jamais mis à jour au renommage Mothana→Samakan (staging, créé après le renommage, était correct dès le départ). Supabase rejette silencieusement un `redirectTo` absent de la allow list et retombe sur le `site_url` par défaut, quel que soit le domaine réellement utilisé par l'appelant — d'où le lien vers `mothana.vercel.app` même en partant de `samakan.fr`. **Corrigé en config prod** via `PATCH /v1/projects/{ref}/config/auth` (`site_url` → `https://samakan.fr`, ajout de `samakan.fr/**` et `www.samakan.fr/**` à la allow list, `mothana.vercel.app/**` conservé en plus sur décision utilisateur — alias toujours fonctionnel). Effet immédiat, indépendant du merge de code. Confirmé par l'utilisateur en prod (lien reçu = samakan.fr).
  - **Blocage rencontré puis levé** : le premier appel Management API (lecture de la config prod) a été bloqué par le classifier auto-mode (même comportement que pour l'accès direct à la base prod, cf. promotion PR #151-156) — débloqué après désactivation du mode auto par l'utilisateur.
  - **PR #160 conservée en complément** (pas la cause principale, mais un vrai risque résiduel) : `getCanonicalSiteUrl()` ajouté dans `src/lib/environment.ts`, force `https://samakan.fr` en prod quel que soit l'alias (`mothana.vercel.app` restant valide dans la allow list) depuis lequel l'action est déclenchée ; comportement recette/local inchangé (`isRecette()` garde `window.location.origin`). Appliqué aux 3 points d'appel identifiés (reset password, invitation admin, lien de désinscription mailing — même pattern `site_url: window.location.origin`).
  - Carte Trello déplacée en Done.

- **Campagne courrier & mail : aperçu nominatif des destinataires** (terminé 2026-09-12, PR #162 dev, mergée, batch "Batch 333") : bouton "Voir la liste (N)" à côté du compteur existant sur `CampagneCourrierPage` et `CampagneMailingPage`, ouvrant une modale en lecture seule listant les adhérents actuellement inclus par le filtre/la liste sélectionnée (nom/prénom/adresse pour le courrier, nom/prénom/email pour le mail). Clic sur une ligne → ouvre la fiche adhérent en édition (`AdherentModal` embarqué, même pattern que la modale bounces du dashboard), fetch de l'enregistrement complet à la demande plutôt que de tout charger dans l'aperçu. Duplication assumée entre les deux pages (règles métier différentes : opt-out mail, prédicat "complet" propre à chaque canal) — factorisation envisagée puis écartée en cadrage, notée en dette technique séparée.
  - **Bug mobile trouvé en testant, corrigé dans la même PR** : 3 colonnes (Nom/Prénom/Adresse ou Email) ne tenaient pas dans la largeur d'une modale sur mobile — la colonne utile (adresse/email) coupée sans indication de scroll. Fusion Nom+Prénom en une colonne "Destinataire" (2 colonnes) + retour à la ligne forcé sur les emails longs (`break-all`) : tient sans scroll horizontal sur mobile, desktop inchangé.

- **Listes adhérents : gestion des listes** (terminé 2026-09-12, PR #163 dev, mergée, batch "Batch 333") : modale "Gérer les listes" depuis `AdherentsPage` (bouton à côté de "Nouvelle liste") — renommer (inline), vider (retire tous les adhérents, liste conservée) et supprimer, avec confirmation listant nominativement les adhérents impactés avant de valider. 4 RPC (`gestion_listes_diffusion.sql`) : `list_listes_diffusion_avec_compte`, `renommer_liste_diffusion` (bloqué avec message si le nom cible existe déjà), `vider_liste_diffusion`, `supprimer_liste_diffusion` — mises à jour en masse sur `adherents.tags` (`array_replace`/`array_remove`), pas de boucle par adhérent.
  - **4 retours utilisateur en testant, tous traités dans la même PR** : (1) 3 colonnes (Nom/Adhérents/Actions texte "Vider"/"Supprimer") débordaient sur mobile — boutons remplacés par des pictogrammes (cercle barré / poubelle, `title`+`aria-label` conservés) et colonne Adhérents fusionnée sous le nom de liste, tient exactement en largeur sur mobile sans scroll. (2) Confirmation Vider/Supprimer se contentait de rappeler le nombre d'adhérents déjà visible juste au-dessus (redondant) — remplacé par la liste nominative réelle (fetch à l'ouverture de la confirmation), scrollable au-delà de quelques noms. (3) Aucun retour visuel après une action réussie — toast ajouté sur les 3 actions (même composant `Toast` que le reste de l'app). (4) Le renommage au clic sur le nom n'était signalé qu'au survol (`hover:underline`, invisible sur mobile) — icône crayon ajoutée à côté du nom, toujours visible, même geste inchangé.
  - **Bug transverse trouvé au passage, corrigé dans la même PR** : `Toast` (composant partagé par toute l'app) et `DialogOverlay` partageaient le même `z-index` (`z-50`) — l'overlay, monté après dans le DOM, passait devant et assombrissait le toast quand une modale restait ouverte après l'action. `Toast` remonté à `z-[60]` dans le composant lui-même (pas un correctif local à cette modale) — corrige le même souci partout ailleurs où toast et modale peuvent coexister.

- **Statuts non configurés : checkbox conditionnelle + bannière rappel admin** (terminé 2026-09-12, PR #164 dev, mergée, batch "Batch 333") : `DemandeAdhesionPage.tsx` — la case "J'ai pris connaissance des statuts…" ne s'affiche (et n'est exigée) que si `organisation.statuts_url` est renseigné, sinon rien à faire accepter. Point d'attention géré : `demandes_adhesion.accepte_statuts` a une contrainte `CHECK (accepte_statuts = true)` en base — sans la case, `accepte_statuts` forcé explicitement à `true` côté insert pour ne pas violer la contrainte. `DashboardPage.tsx` : 4e bannière ambre de rappel admin (même pattern que les 3 bannières d'action déjà en place), visible si `statuts_url` absent, lien direct vers `/admin/parametres/adherents`. Vérifié de bout en bout (soumission complète du formulaire public sans la case, "Demande envoyée" sans erreur RLS/CHECK).

- **Page Adhérents : retirer en masse une liste sur la sélection courante** (terminé 2026-09-12, PR #165 dev, mergée, batch "Batch 333") : bouton "Retirer d'une liste" symétrique au "Ajouter à une liste" existant, visible dès qu'au moins un adhérent est sélectionné. La modale liste uniquement les listes présentes parmi les adhérents sélectionnés (union de leurs `tags`, calculée côté client, pas de requête supplémentaire), confirmation affichant le nombre réellement concerné parmi la sélection. Nouvelle RPC `remove_adherents_tag`, symétrique à `add_adherents_tag` (`array_remove` en une requête batch, ne touche pas au registre `listes_diffusion`).
  - **Bug mobile trouvé en testant, corrigé dans la même PR** : le groupe de boutons de la barre de sélection multiple ("Ajouter à une liste"/"Retirer d'une liste"/"Imprimer les cartes") n'avait pas `flex-wrap` — débordait hors du conteneur sur mobile au lieu de passer à la ligne. Ajout de `flex-wrap`, cohérent avec le conteneur parent qui l'avait déjà.
  - **Conflit de fusion rencontré et résolu** : cette branche et la PR #163 ("Gestion des listes") étaient toutes les deux parties de `dev` avant que #163 ne soit mergée en premier — les deux modifiaient `AdherentsPage.tsx` indépendamment (import, state, embed de modale). Résolu en conservant les deux fonctionnalités (imports, state et embeds `RetirerListeModal` + `GererListesModal` côte à côte), revérifié en navigateur après fusion que les deux cohabitent sans régression.
