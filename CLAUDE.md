# CLAUDE.md — Mothana (Gestion des dons)

Ce fichier est lu automatiquement par Claude Code à chaque session. Il contient le contexte du projet, les conventions à respecter, et l'état d'avancement.

---

## Session Continuity

### En début de session
- Chercher automatiquement le fichier de la dernière session dans `.claude/sessions/`
- Identifier où on s'est arrêté et les blockers en cours
- Résumer en 3 lignes avant de commencer
- Vérifier la liste "Todo" du board Trello "Mothana" (voir mémoire persistante pour les identifiants/credentials) — l'utilisateur y note à la volée ses demandes d'évolution. En cas de nouveauté (carte non présente dans le backlog de ce fichier), proposer de la cadrer pour l'inscrire dans "État d'avancement" / backlog ci-dessous
- Quand un sujet est cadré (que la carte Trello associée soit une nouveauté détectée ou un sujet déjà connu qu'on approfondit), alimenter la carte Trello correspondante avec le détail du cadrage (description), réécrire le titre si le libellé d'origine est devenu imprécis une fois le sujet creusé, et appliquer l'étiquette verte "cadré" (signal fiable, ne pas se fier uniquement à la présence d'une description)
- À ce même moment, reclasser les cartes de "Todo" par priorité/complexité (jugement de l'agent) : un sujet peu complexe peut remonter en haut de la liste, un sujet complexe redescend en général plus bas — sauf s'il est aussi sensible ou prioritaire, auquel cas il remonte malgré sa complexité
- Ce réflexe est aussi noté dans la mémoire persistante (MEMORY.md) pour qu'il s'applique même si ce fichier n'est pas relu

### En fin de session
- Sauvegarder un résumé dans `.claude/sessions/[date]_[sujet].md`
- Inclure : Réalisé, Reste à faire, Blockers, Décisions
- Si un fichier existe déjà pour aujourd'hui, le compléter plutôt que le remplacer
- Format du nom de fichier : `YYYY-MM-DD_sujet-en-kebab-case.md`
- Exemple : `2026-07-17_refonte-cerfa.md`
- **Déplacement Trello Done ↔ résumé "Terminé" de ce fichier : action indissociable, jamais l'une sans l'autre.** Dès qu'une carte passe en Done (fin de sujet ou merge de PR), ajouter dans la même respiration l'entrée résumé correspondante dans "État d'avancement" ci-dessous — ne jamais différer à une session ultérieure (c'est exactement ce qui a créé une désynchro le 2026-08-15 : carte Trello "impeccable" en Done depuis le 2026-08-12 sans entrée correspondante dans ce fichier, découverte seulement au check bidirectionnel 3 jours après)

### Règles
- Toujours lire AVANT d'agir — ne pas redemander ce qui est déjà documenté
- Les blockers non résolus de la session précédente deviennent la priorité
- Quand un blocker est levé, le noter explicitement dans "Réalisé"
- **Avant toute action corrective sur une carte Trello jugée mal classée** (ex. carte en Done qui semble ne pas avoir été terminée, ou l'inverse) : `grep` l'URL/le nom de la carte sur **l'ensemble** de `.claude/sessions/*.md`, pas seulement le fichier de session le plus récent, pour reconstituer l'historique complet avant de conclure et d'agir — un sujet peut se refermer dans une session ultérieure à celle qui l'a initialement cadré

---

## Contexte du projet

Mothana est une application de gestion des dons pour associations. C'est un MVP fullstack (React + Supabase) construit à partir d'une maquette existante.

**Lire impérativement avant toute action :**
- `docs/cadrage-mothana.md` — spec fonctionnelle complète
- `docs/schema-mothana.sql` — schéma SQL de référence
- `docs/plan-dev-mothana.md` — plan de développement
- `docs/regles-recus-fiscaux.md` — règles métier reçus fiscaux (Cerfa)
- `docs/brief-cerfa.md` — brief technique complet refonte Cerfa (priorité en cours)

---

## Stack technique

- **Frontend** : React + TypeScript + Vite, Tailwind CSS, React Router
- **Backend** : Supabase (PostgreSQL + Auth + Storage + Edge Functions)
- **Client JS** : `@supabase/supabase-js`
- **Hébergement** : Vercel Pro (frontend) + Supabase Pro (backend)
- **Génération PDF** : Gotenberg (HTML→PDF, à déployer sur Railway/Render ~5$/mois) — remplace pdf-lib

---

## Environnement de développement — ⚠️ règle critique

Claude Code tourne sur une machine dédiée où le projet est exposé sur le réseau via une instance **permanente** `npm run dev` (port 5173) — c'est cette URL que l'utilisateur utilise pour piloter/vérifier le travail à distance (MacBook ou smartphone). Cette instance ne doit **jamais** être interrompue par l'agent.

- **Ne jamais faire `pkill -f vite`** (ou tout kill par nom de processus) — ça tue l'instance permanente de l'utilisateur, pas seulement une instance de test lancée par l'agent
- Pour des vérifications visuelles (Playwright/screenshots), **réutiliser l'instance déjà en cours sur `http://localhost:5173`** plutôt que d'en lancer une nouvelle — les changements de fichiers sont pris en compte automatiquement (HMR Vite)
- Si un test nécessite exceptionnellement une route/fichier temporaire (ex: harnais de test pour un composant), l'ajouter, tester via l'instance existante, puis le retirer avant de commiter — sans jamais démarrer ni arrêter de processus `npm run dev`/`vite`
- Si une instance séparée est vraiment nécessaire, la lancer sur un port dédié différent et ne l'arrêter que par PID exact (jamais par pattern de nom)
- Skill `.claude/skills/webapp-testing/` disponible pour les vérifications navigateur (Playwright Python) — son propre helper `scripts/with_server.py` sait démarrer/arrêter un serveur, mais **ne pas l'utiliser dans ce projet** puisqu'une instance tourne déjà en permanence : suivre la branche "serveur déjà en cours → reconnaissance puis action" de son arbre de décision, jamais la branche "démarrer un serveur"

---

## Modèle d'authentification

**Super-Admin** : `is_super_admin = true` dans `app_metadata` auth.users → `/super-admin`
**Admin** : Supabase Auth email/password → dashboard organisation via `profils_organisation`
**Bénévole** : PIN → Edge Function `verify-pin` → `signInWithPassword` compte technique `benevole-{org_id}@mothana.internal`

⚠️ Pas de table `utilisateurs_app` — tout via `auth.users` Supabase
⚠️ JWT custom abandonné (RS256 incompatible HS256)

---

## Schéma de données

Référence complète et à jour (toutes les tables, colonnes, contraintes) : `docs/schema-mothana.sql`. Points non évidents à la simple lecture des colonnes :

- `personnes.civilite` (smallint) : 1=Monsieur 2=Madame 3=Mademoiselle 4=Foyer 5=Société 6=Association 7=Famille — 0/255→NULL
- `adherents.civilite` (smallint réduit, enum **distinct** de celui de `personnes`) : 0=non défini, 1=Monsieur, 2=Madame — pas de personne morale/famille adhérente pour l'instant
- `profils_participant.id_externe` = IDFideles (import legacy, seulement rempli pour les participants importés) — ne pas l'utiliser comme numéro de donateur générique
- `adhesions.mode_paiement` réutilise l'enum `dons.mode_paiement` (CHECK `[1,2,3,4]` : Espèces/Chèque/Prélèvement-virement/Autres)
- `adhesions.renouvellement` et `date_fin` : calculés côté application (`lib/adhesion.ts`), jamais saisis manuellement

---

## Conventions de code

- **Langue** : code en anglais, UI en français
- **Composants** : PascalCase, un fichier par composant
- **Hooks custom** : préfixe `use`, dans `src/hooks/`
- **Types TypeScript** : dans `src/types/`, toujours typer les réponses Supabase
- **Pas de `any`** sauf cas exceptionnel justifié en commentaire
- **Réutilisation** : composants partagés entre écrans (formulaires, modales, autocomplete)

---

## Sécurité — règles absolues

- Sécurité via **RLS Supabase**, pas uniquement côté frontend
- Ne jamais exposer la clé `service_role` côté client
- Ne jamais commiter `.env`

---

## Git — workflow

Règles génériques (ne jamais merger sans confirmation, vérifier les PR ouvertes avant de démarrer, corriger un blocage trouvé en testant dans la même PR, ouvrir une PR dès qu'un dev est jugé terminé) : voir `~/.claude/CLAUDE.md`, valables sur tous les projets.

Spécifique à Mothana :
- Quand l'utilisateur informe qu'une PR de feature est mergée (cible `dev`), `checkout dev` puis `pull` pour mettre la branche locale à jour avant de démarrer les développements suivants — `checkout main`/`pull` seulement après une promotion `dev` → `main` (voir plus bas). Vérifier aussi à ce moment-là la liste "Todo" du board Trello "Mothana" (voir mémoire persistante) : identifier les cartes traitées par cette PR, les déplacer soi-même vers "Done" (le token a les droits d'écriture depuis le 2026-08-08), et signaler à l'utilisateur les cartes déplacées + toute nouvelle carte apparue depuis le dernier check, pour discuter d'éventuels nouveaux sujets — vérification **bidirectionnelle** (voir mémoire persistante), pas seulement un scan des nouvelles cartes
- **Branche `dev` (depuis le 2026-08-15, environnement de recette)** : les PR de features ciblent désormais `dev`, plus `main` directement. `main` ne reçoit que des promotions depuis `dev` (une feature à la fois, pas de lot) — c'est ce merge `dev → main`, pas le merge feature→`dev`, qui déclenche migrations Supabase + déploiement Edge Function en prod. Détail complet : `docs/environnement-recette.md`. **`CLAUDE.md` n'a plus de règle spéciale** (revu le 2026-08-21, après une dérive constatée entre les deux branches causée par cette exception) : il s'édite comme n'importe quel fichier, sur `dev`, et suit le flux normal de promotion vers `main` — `main` peut donc être temporairement en retard de quelques jours sur le backlog/l'historique documenté entre deux promotions, sans conséquence puisque `CLAUDE.md` n'est pas exécutable (pas de migration/déploiement en jeu).
- **Promotion `dev` → `main` via PR GitHub (depuis le 2026-08-15)** : `git merge` direct bloqué par le classifier auto-mode sur des actions jugées sensibles sur `main` — la promotion passe donc par `gh pr create` (base `main`, head `dev`) puis `gh pr merge --merge` (commit de fusion, jamais `--squash`/`--rebase`, pour garder l'équivalent `--no-ff`). Exception scopée à *cette* PR de promotion : sur confirmation explicite de l'utilisateur pour démarrer la promotion, l'agent peut créer **et** merger cette PR sans redemander — pas de re-review de contenu à ce stade (déjà validé au merge feature→dev). Ne s'applique **pas** aux PR de feature classiques (feature→`dev`), qui restent soumises à la règle standard "jamais merger sans autorisation explicite" (`~/.claude/CLAUDE.md`).
  - ⚠️⚠️⚠️ **NE JAMAIS laisser `gh pr merge` supprimer la branche `dev`** (option par défaut sur une PR de feature, à désactiver explicitement ici — `--delete-branch=false` ou décocher dans le flux interactif). `dev` est une branche **permanente**, pas une branche de feature jetable : la recette (`test.samakan.fr`) et les variables d'environnement Vercel scopées y sont rattachées par son *nom*. Une suppression basculerait silencieusement la recette sur les identifiants **prod**, sans erreur visible. ⚠️⚠️⚠️

---

## État d'avancement

Historique complet des sujets terminés (détail des décisions techniques, bugs trouvés en testant, PR associées) : `docs/journal-avancement.md` — à lire seulement en cas de besoin d'archéologie sur une décision passée, pas systématiquement.

### ✅ Terminé (résumé — détail dans le journal)

- MVP + Post-MVP (étapes 0–9, comptes admin, imports, pagination, accessibilité modales…)
- **Priorité 1 — Refonte Cerfa** (terminée 2026-07-18) : migrations, paramètres organisation, templates HTML par défaut, edge function `generate-recu` (Gotenberg), UI reçus fiscaux, gestion des templates. Brief technique complet : `docs/brief-cerfa.md`
- **Priorité 3 — Wizard de template Cerfa** (terminée 2026-07-20) : upload PDF → Claude Vision → brouillon HTML/CSS dans l'éditeur Monaco. Assets par organisation (liste ouverte) + placeholders président (PR #35)
- **Priorité 2 — Export comptable** (terminé, périmètre réduit le 2026-08-17) : export CSV dons, tableau de bord `/admin/comptabilite`, récapitulatif déclaratif article 222 bis CGI. Le 4e item (rapprochement chèques/virements) abandonné — pas d'obligation légale pour une petite association, pas de vrai besoin utilisateur identifié, carte Trello archivée
- Backlog 2026-07-25 (5 items, PR #31→#34) : affichage dons participant, perf Wat Velouvanaram (RLS `auth_rls_initplan`), recherche + pagination activités, réaffectation de don (blocage si reçu déjà émis)
- **Module Adhérents V1** (terminé 2026-08-04, PR #36→#39) : navigation (dashboard + section Adhérents), formulaire/liste/import, gabarit carte adhérent + planche A4. Item 6 (sélection d'un adhérent à la saisie d'un don) explicitement hors scope, backlog actif ci-dessous
- Recherche adhérents par email + `id_externe` auto-incrémenté (PR #47/#48)
- Log des modifications (`journal_modifications`) + motif de refus demande d'adhésion (PR #49)
- Marathon Trello 2026-08-08 : CTA ratifier/refuser (PR #50), lignes de tableau cliquables — **convention actée pour tout nouveau tableau du même type** (PR #51), validation date de naissance adhérent (PR #52), débordement impression cartes adhérent (PR #53), message de succès formulaire d'adhésion (PR #54)
- **Environnement staging Supabase** (terminé 2026-08-10) : projet `mothana-staging` séparé de prod, schéma/Edge Functions/secrets répliqués, comptes de test créés, workflow dump/restore par sujet documenté dans `docs/environnement-staging.md`. Bug corrigé au passage : création d'organisation cassée en prod depuis le 2026-08-05 (`organisations.slug` non renseigné)
- **Revue UI responsive admin** (terminée 2026-08-11, PR #57) : 6 débordements mobile corrigés (table SuperAdminPage, header dupliqué dans 5 modales/pages, champ slug Paramètres), audit Playwright systématique 3 largeurs + données de test réalistes injectées dans "Association Démo Staging" (staging, conservées pour les prochains audits). Skill design QA `impeccable` (pbakaus/impeccable) installé en scope utilisateur au passage
- **Revue UI responsive admin avec le skill impeccable** (terminée 2026-08-12, PR #58) : `PRODUCT.md`/`DESIGN.md` générés (init + document), extraction `SectionHeader.tsx` (5 fichiers dupliqués), balayage exhaustif Playwright (débordement 320/390/768/1440px sur 9 pages admin + super-admin + bénévole + 20+ modales + 3 pages publiques) et axe-core WCAG AA (0 violation après correctifs contraste texte 113 occurrences/38 fichiers + labels de formulaire manquants). Détail complet sur la [carte Trello](https://trello.com/c/0cgE88pI)
- **Campagnes de mailing Brevo** (terminée 2026-08-15, PR #62) : config Brevo par organisation (clé API + expéditeur), page `/admin/adherents/mailing` (composition TipTap gras/lien/placeholders `{{params.prenom}}`/`{{params.nom}}`, pièce jointe 10 Mo max transmise en base64, filtre destinataires actif/archivé/tous, confirmation avant envoi, brouillon persistant en localStorage, historique), Edge Function `send-mailing-brevo` (mode batch natif Brevo `messageVersions`, jusqu'à 1000 destinataires/appel)
- **Tooltip stylisé sur les placeholders** (terminée, PR #59) : remplace le `title` natif par `Tooltip.tsx` (prop `bare` ajouté) sur les 3 éditeurs de template (Cerfa, carte adhérent, formulaire d'adhésion)
- **Réorganisation Paramètres** (terminée, PR #60) : 4 sous-pages routées (`/admin/parametres`, `/fiscal`, `/adherents`, `/suivi`), composant partagé `ParametresSection.tsx`
- **Environnement de recette** (terminé 2026-08-15) : `test.samakan.fr` (branche `dev`, réutilise le projet Supabase `mothana-staging`, isolation par organisation dédiée par client), nouveau workflow git `dev`→`main` pour la promotion vers prod. Détail complet : `docs/environnement-recette.md`
- **Email d'invitation admin + mot de passe oublié via Resend** (terminé 2026-08-15, PR #63 dev, PR #64 promotion prod) : `create-admin` génère un lien d'invitation (`auth.admin.generateLink`) au lieu d'un mot de passe en clair, nouvelle Edge Function `request-password-reset` (réponse générique anti-énumération), les deux envoyées via Resend (template HTML Mothana). Un seul compte Resend pour dev/recette/prod (domaine `samakan.fr` vérifié). Validé de bout en bout sur la recette avant promotion
- **Désinscription mailing (RGPD)** (terminé 2026-08-18, PR #65 dev, PR #66 promotion prod le 2026-08-19) : opt-out par lien (`adherents.mailing_opt_out`/`mailing_opt_out_at`/`mailing_unsubscribe_token`), route publique `/desinscription?token=...` + Edge Function `unsubscribe-mailing` (réponse générique anti-énumération), footer de désinscription injecté automatiquement par `send-mailing-brevo` (lien personnalisé par destinataire via les params Brevo), toggle "Ne pas contacter par email" dans `AdherentModal`. Bug corrigé au passage : la RPC `search_adherents` (liste adhérents) ne renvoyait pas les nouveaux champs, laissant la checkbox toujours décochée à l'ouverture. Validé de bout en bout sur staging puis sur la vraie recette (`test.samakan.fr`, envoi réel via Brevo, exclusion confirmée par les logs Brevo, désinscription testée par l'utilisateur lui-même et re-vérifiée par l'agent sur le vrai déploiement avant promotion)
- **Domaine samakan.fr (apex + www) sur la prod Vercel** (terminé 2026-08-19) : `samakan.fr` et `www.samakan.fr` servent désormais la prod directement (aucune redirection entre eux, `mothana.vercel.app` continue de fonctionner en parallèle). Accès API OVH mis en place au passage (clé scopée `/domain/zone/samakan.fr/*` uniquement, voir mémoire persistante) — a permis de diagnostiquer et corriger 3 bugs DNS que l'interface Manager ne permettait pas de résoudre (échec de suppression bloquant) : doublon d'enregistrement `A` à l'apex (ancien parking OVH `51.91.236.255` jamais retiré, coexistant avec l'IP Vercel en round-robin), `AAAA` résiduel pointant vers le même parking, et redirection 308 apex→www activée par défaut côté Vercel (désactivée, contraire à la décision actée). Débloque "Renommage public en Samakan".
- **Renommage public en "Samakan"** (terminé 2026-08-19, PR #67 dev, PR #68 promotion prod) : nom de projet "Mothana" inchangé (code, docs, Supabase, Trello, dépôt Git), "Samakan" devient le nom affiché à l'écran — 10 endroits (titre d'onglet, titres d'écrans publics, en-têtes sidebar admin/super-admin, en-tête bénévole, texte de conformité Cerfa, placeholders d'exemple nom d'organisation remplacés par un exemple générique). Périmètre cadré le 2026-08-12 (9 endroits) + 1 endroit ajouté (`DesinscriptionMailingPage.tsx`, créée après le cadrage initial).
- **Vérification carte adhérent par nom/prénom (espace bénévole)** (terminé 2026-08-19, PR #69 dev, PR #70 promotion prod) : nouvel onglet "Vérifier un adhérent" dans `/benevole` (à côté de "Saisir un don"), réutilise l'auth PIN existante. RPC dédiée `search_adherents_verification` (pas la RPC `search_adherents` existante) : même logique de recherche par tokens nom+prénom, mais ne renvoie que nom/prénom/statut/date de fin de validité — jamais adresse/téléphone/email, même pour un bénévole authentifié (vérifié par inspection réseau directe de la réponse RPC). Recherche vide = aucun résultat (pas de listing complet).
- **Page de tour produit `/decouvrir`** (terminé 2026-08-21, PR #71 dev, PR #72 promotion prod) : tour produit pour clients/prospects, 5 sections (Dons, Adhérents, Mailing, Espace bénévole, Personnalisation), sommaire à ancres, captures réelles encadrées façon navigateur. Initialement nommée `/documentation`, renommée après relecture utilisateur (galerie/accroches marketing ≠ vraie doc instructionnelle — scindé en carte séparée "Vraie documentation utilisateur", pas encore cadrée). Captures prises sur l'organisation "Association Démo Staging" (staging) après purge complète des données réelles et régénération d'un jeu 100% fictif. Bug de confidentialité trouvé et corrigé en cours de route : la config Brevo de l'organisation démo exposait une vraie adresse email personnelle de l'utilisateur, remplacée par une valeur fictive avant capture.
- **Corriger le branding Mothana→Samakan dans les emails** (terminé 2026-08-21, PR #73 dev, PR #74 promotion prod) : `create-admin`/`request-password-reset` (titre, sujet, expéditeur). Amélioration ajoutée en testant sur Yopmail : l'email d'invitation admin précise désormais le nom de l'organisation concernée (récupéré côté serveur, pas transmis par le frontend). En testant cette PR, un blocage plus large a aussi été détecté et cadré séparément (créer un admin avec un email déjà utilisé par une autre organisation) : voir "Support multi-organisation pour un compte admin" dans le backlog actif ci-dessous.
- **Bandeau visuel distinguant l'environnement recette de la prod** (terminé 2026-08-21, PR #75 + #76 dev, PR #77 promotion prod) : bannière ambre "Environnement de test", détection par `window.location.hostname`. Périmètre initial (`AdminLayout` + `BenevolePage`) étendu en PR #76 après un gap trouvé par l'utilisateur en testant sur la vraie recette : ajouté aussi sur `SuperAdminLayout` et la mire de connexion (`HomePage`).
- **Identifier l'espace bénévole + l'organisation dans le header** (terminé 2026-08-22, PR #78 dev, PR #83 promotion prod) : header `/benevole` sur 2 lignes, ligne 1 inchangée ("Samakan" + Quitter), ligne 2 "Espace bénévole — *Organisation*" (même pattern de fetch que `AdminLayout.tsx`). Vérifié de bout en bout sur la recette via le vrai flux PIN bénévole. Promotion faite par cherry-pick du commit de code sur une branche dédiée (pas un `dev → main` direct) pour garder les promotions séparées malgré les 4 autres features déjà mergées sur `dev` depuis — les commits `docs:` associés restent sur `dev` uniquement, sans conséquence (`CLAUDE.md` n'est pas exécutable).
- **Afficher le numéro d'adhérent dans la vérification bénévole** (terminé 2026-08-22, PR #79 dev, PR #84 promotion prod) : `search_adherents_verification` renvoie désormais `id_externe` (numéro d'adhérent, pas une donnée sensible), affiché "N° adhérent : …" dans `BenevoleVerificationAdherent.tsx`, masqué si vide. Vérifié de bout en bout sur la recette via le vrai flux PIN bénévole. Promotion par cherry-pick (même méthode que PR #83) + migration `search_adherents_verification_id_externe.sql` rejouée sur prod, fonction vérifiée présente après coup.
- **Corriger le calcul de hauteur mobile (100vh → dvh)** (terminé 2026-08-22, PR #80 dev, PR #85 promotion prod) : `min-h-screen`/`h-screen` remplacés par les utilitaires `dvh` de Tailwind sur 11 fichiers/14 occurrences (`w-screen` non touché, pas concerné par le problème). Blocage trouvé par l'utilisateur en testant sur un vrai téléphone (barre d'adresse Android Chrome) : la modale par défaut de `Modal.tsx` (`max-h-[90vh]`, hors périmètre du grep initial car syntaxe Tailwind arbitraire) avait le même symptôme — haut inaccessible au scroll, combiné au centrage flex du composant. Corrigé dans la même PR (`max-h-[90dvh]`), composant partagé donc toutes les modales de l'app en bénéficient. Promotion par cherry-pick (même méthode que PR #83/#84), pas de migration.
- **Garde-fou de suppression d'organisation** (terminé 2026-08-22, PR #81 dev, PR #86 promotion prod) : comptage `adherents` ajouté (à côté de donateurs/dons déjà calculés), modale de suppression affiche les 3 chiffres, confirmation façon GitHub (saisie du nom exact pour activer le bouton). Étendu en cours de route sur demandes de l'utilisateur : CTA "Modifier" retiré de la ligne (déjà cliquable, doublon), "Supprimer" déplacé dans le pied de la modale d'édition ; modale "Admins" fusionnée dans "Modifier l'organisation" (CTA "Admins" retiré) ; CTA "Consulter" remplacé par un picto œil ; toast de confirmation après suppression réussie (réutilise `Toast`/`useToast` existant). Vérifié de bout en bout sur staging (super-admin réel), y compris via une organisation jetable créée puis supprimée pour ne pas toucher aux données de test existantes. Promotion par cherry-pick (même méthode que PR #83/#84/#85), pas de migration.
- **Nettoyer la dette lint** (terminé 2026-08-22, PR #82 dev, PR #87 promotion prod) : 34 → 0 problème. Le gros morceau (28× `react-hooks/set-state-in-effect`) s'est révélé être un faux positif documenté de la règle sur le pattern fetch-au-montage (recommandé par la doc React elle-même, cf. [react/react#34743](https://github.com/react/react/issues/34743)) — écarté du cadrage initial : désactivé dans `eslint.config.js` plutôt que contourné par ~28 hacks `setTimeout`. Les 6 restants traités au cas par cas (pas d'ajout aveugle de dépendance) : `useAuth` extrait dans `src/hooks/useAuth.ts` + `AuthContext`/`AuthContextValue`/`AuthState` dans `src/contexts/authContextInstance.ts` (Fast Refresh), `fetchActivites`/`loadData` passées en `useCallback`, 2 assignations mortes retirées, 1 dépendance volontairement exclue (documentée en commentaire). Vérifié : eslint 0 problème, typecheck, build prod, et les 3 flux d'auth (admin/super-admin/bénévole) sur staging. Promotion par cherry-pick (même méthode que PR #83-86), pas de migration — dernière des 5 promotions du 2026-08-22, `dev` et `main` de nouveau alignés côté code (seuls les commits `docs:` restent propres à `dev`).

- **Envoi du reçu fiscal par email au donateur** (terminé 2026-08-22, PR #90 dev, PR #91 promotion prod) : bouton "Email" à côté de "PDF" dans `RecusFiscauxPage.tsx` (masqué si le participant n'a pas d'adresse email), nouvelle Edge Function `send-recu-email` (télécharge le PDF depuis le bucket privé `recus-fiscaux`, l'attache en base64 via Resend, pose `recus_fiscaux.email_envoye_at` — colonne déjà en base depuis la refonte Cerfa, jamais utilisée jusqu'ici, affichée "Envoyé le …" dans le tableau). Portée individuelle, déclenchement manuel, un envoi à la fois. Limitation super-admin confirmée en testant (voir plus bas), erreur réseau ponctuelle sur mobile/VPN de l'utilisateur non reproduite (probablement liée à l'environnement local de test, pas un bug de l'app). Déployée et testée de bout en bout sur staging avant merge. Promotion par cherry-pick (même méthode que les précédentes), pas de migration (colonne déjà présente en prod, vérifié directement en base) — Edge Function déployée explicitement sur prod après le merge de promotion.

- **Toast de confirmation après ajout d'un compte admin** (terminé 2026-08-22, PR #92 dev, PR #94 promotion prod) : nouveau prop `onAdminAdded` sur `OrgModal` (même pattern que `onSaved`/`onDeleteRequest`), réutilise le `Toast`/`useToast` déjà en place dans `SuperAdminPage.tsx`. Vérifié de bout en bout sur staging (super-admin réel, compte de test créé puis désactivé). Promotion par cherry-pick, pas de migration.
- **Reçus fiscaux : champ de recherche par nom** (terminé 2026-08-23, PR #93 dev, PR #95 promotion prod) : réutilise le helper existant `matchesParticipantSearch` (`src/lib/participantSearch.ts`), même emplacement/style que le champ de recherche d'`ActivitesPage.tsx`. Filtrage client-side, pagination et compteur mis à jour sur le résultat filtré, état "Aucun résultat" distinct de "Aucun don enregistré". Vérifié visuellement (résultat, sans résultat, champ masqué si aucun don sur l'année). Promotion par cherry-pick, pas de migration.
- **Listes de diffusion (tags adhérents)** (terminé 2026-08-25, PR #96 dev, PR #97 promotion prod) : colonne `adherents.tags text[]` (appartenance) + table `listes_diffusion` (registre des noms de liste par organisation, alimentée automatiquement par trigger dès qu'un tag est utilisé) — pivot d'architecture décidé en cours de dev sur retour utilisateur, pour permettre de créer une liste vide sans adhérent présélectionné (le cadrage initial prévoyait "pas de table dédiée", explicitement abandonné). Affectation en masse depuis `AdherentsPage.tsx` (bouton "Nouvelle liste", RPC batch `add_adherents_tag`), nuage de tags éditable dans `AdherentModal.tsx` (`TagsInput.tsx` partagé), filtres liste + exclusion regroupés visuellement, sélecteur "Envoyer à" unifié statut/liste sur `CampagneMailingPage.tsx` (une liste prime sur le statut), `send-mailing-brevo` étendu. Bug de build Vercel trouvé et corrigé en cours de route (prop `availableTags` rendue obligatoire à tort, cassait `DemandesAdhesionPage.tsx` qui réutilise aussi `AdherentModal`) — détecté par le déploiement, pas par `tsc --noEmit` local (config racine sans project references sans `-b`, désormais `tsc -b` utilisé pour matcher la commande Vercel). Vérifié de bout en bout sur la recette (Playwright) à 2 reprises (avant et après le pivot table). Promotion par cherry-pick des 7 commits de la PR #96 (une seule migration en 2 fichiers séquentiels, ordre respecté : `adherents_tags.sql` puis `listes_diffusion.sql`) + déploiement explicite de `send-mailing-brevo` sur prod — dump prod pris avant migration (`~/dump_prod_avant_listes_diffusion_20260825.sql`).
- **Configuration Brevo : CTA + modale de configuration** (terminé 2026-08-25, PR #98 dev, PR #99 promotion prod) : formulaire (clé API, expéditeur) sorti de `CampagneMailingPage.tsx` vers `BrevoConfigModal.tsx`, indicateur "Configuré"/"Non configuré" + bouton "Configurer" sur la page, bouton "Enregistrer" affiché seulement en état "dirty" (au moins un champ différent des valeurs chargées). Validation live du format email de l'expéditeur ajoutée en cours de revue (même pattern `isValidEmail` que `AdherentModal.tsx`, absente du cadrage initial). Vérifié sur staging (Playwright). Promotion par cherry-pick des 2 commits de la PR #98, pas de migration ni Edge Function concernée.
- **Mailing : joindre plusieurs fichiers (max 3)** (terminé 2026-08-25, PR #100 dev, PR #102 dev, PR #103 promotion prod) : `send-mailing-brevo` envoyait déjà un tableau d'attachments à Brevo, seul le frontend limitait à 1 fichier. `pieceJointe` → `piecesJointes[]`, payload `piece_jointe` → `pieces_jointes`, plafond 10 Mo par fichier conservé, input masqué une fois le max atteint. Un blocage Brevo rencontré en testant (expéditeur `contact@samakan.fr` non validé, "Sending has been rejected") — pas un bug Mothana, résolu côté compte Brevo par l'utilisateur (authentification du domaine). Retour utilisateur après ce test : le picker natif n'acceptait qu'un fichier à la fois (Android + Chrome macOS) faute d'attribut `multiple` sur l'`<input type="file">` — corrigé en PR #102 (`handleAttachmentsChange` traite désormais toute la `FileList`). Testé par l'utilisateur, fonctionnel. Promu en prod avec le fix id_externe ci-dessous (PR #103, batch explicitement demandé par l'utilisateur).
- **Fix : collision `id_externe` adhérent après import** (terminé 2026-08-25, PR #101 dev, PR #103 promotion prod, pas de carte Trello — bug ad hoc trouvé par l'utilisateur) : `next_adherent_id_externe()` ne resynchronisait jamais sa séquence sur le vrai max après un import (`import_upsert_adherents` ne touche jamais la séquence), provoquant `duplicate key value violates unique constraint adherents_id_externe_unique`. Fix : `GREATEST(last_value, max réel)` recalculé à chaque appel. Reproduit et vérifié sur staging (séquence à 13 vs max réel 16, puis création réelle réussie). Promotion par cherry-pick des 3 commits (PR #100/#101/#102) en un seul batch sur demande explicite ("Tu peux promouvoir les PR"), migration `next_adherent_id_externe_resync.sql` rejouée sur prod (vérifiée présente) + `send-mailing-brevo` redéployée — dump prod pris avant (`~/dump_prod_avant_mailing_id_externe_fix_20260825.sql`).
- **Fix : compteur de destinataires figé après rechargement (mailing)** (terminé 2026-08-26, PR #104 dev, PR #105 promotion prod) : condition de course entre deux fetches de `fetchDestinatairesCount` au chargement de la page (1 avec la valeur par défaut de `envoyerA`, 1 avec la vraie valeur restaurée du brouillon localStorage) — sans garde, le fetch qui répond en dernier écrase l'autre. Fix : compteur de requête incrémental, seul le résultat du dernier appel lancé est appliqué. Portée confirmée en creusant le bug : l'envoi réel (`handleSend`) recalcule toujours la cible depuis l'état `envoyerA` au clic, jamais depuis ce compteur — aucun risque d'avoir envoyé à la mauvaise liste, seul l'aperçu affiché pouvait être trompeur. Reproduit et vérifié (5/5 tentatives correctes après le fix). Incident DNS sans rapport découvert et corrigé au passage : l'authentification du domaine `samakan.fr` dans Brevo (faite par l'utilisateur pour la validation d'expéditeur) avait ajouté un CNAME sur `test.samakan.fr` pour le tracking de liens/images, écrasant le CNAME existant vers Vercel — recette cassée jusqu'à correction du DNS (voir mémoire `reference_ovh_api.md`). Promotion par cherry-pick, pas de migration ni Edge Function concernée.
- **Peuplement automatique d'une organisation de test (staging)** (terminé 2026-08-26, PR #106 dev, PR #110 promotion prod) : la création d'une organisation via le super-admin, sur staging/recette uniquement (double garde-fou hostname + projet Supabase lié = `mothana-staging`, `src/lib/environment.ts`), insère automatiquement un jeu de données factice fixe (2 activités, 3 donateurs, 5 dons, 4 adhérents à statuts variés actif/archivé) juste après les templates par défaut. Bug RLS trouvé et corrigé au passage : la policy `activites_write_admin` n'avait pas le bypass `is_super_admin` déjà présent sur `dons`/`adherents`/`profils_participant`/`personnes`, bloquant toute écriture d'activité par un super-admin — migration `activites_super_admin_bypass.sql`, rejouée sur prod (sans effet pratique en prod, le peuplement lui-même reste gardé staging-only, mais corrige un vrai bug RLS présent aussi en prod). Vérifié de bout en bout (Playwright + vérification base + contrôle visuel des pages Dons/Adhérents/Activités).
- **Cerfa : détail des dons de l'année** (terminé 2026-08-27, PR #108 dev, PR #110 promotion prod) : nouveau placeholder `{{dons_detail}}` dans `generate-recu` (tableau Événement/Montant/Date/Mode, à intégrer manuellement dans les templates, pas de backfill). Regroupement "Nb Mois" uniquement sur `dons.don_regulier_id` (2 dons ou plus) — pas sur `mode_paiement=3` seul, écarté après analyse des vraies données de prod (99,5% des dons en mode 3 sont uniques dans l'année, indiscernables entre virement ponctuel et engagement historique saisi en une fois). CSS par défaut aligné sur le reste du template (classes `dons-detail`/`dons-detail-total`, nouvelles organisations uniquement). Deux bugs trouvés par l'utilisateur en testant sur son propre template avant merge, corrigés dans la même PR : (1) le placeholder manquait dans la liste de l'éditeur (`cerfaPreview.ts`, simple oubli) ; (2) la colonne Événement restait vide car basée sur `activites.id_externe` (rempli seulement pour les activités importées) — remplacé par `activites.nom`, qui fonctionne dans tous les cas. Amélioration ajoutée au passage : modifier l'activité d'un engagement "Dons réguliers" propage désormais le changement aux dons déjà générés (le montant reste volontairement futurs-uniquement, pour ne pas désynchroniser un reçu fiscal déjà émis).
- **Adhérents mobile : CTA "Sélectionner"** (terminé 2026-08-27, PR #109 dev, PR #110 promotion prod) : bouton "Sélectionner" (mobile uniquement, `sm:hidden`) togglant l'affichage de la colonne checkbox d'`AdherentsPage.tsx` (sélection multiple pour impression de cartes, coûteuse en largeur sur mobile) ; desktop inchangé. Désactiver le mode vide la sélection en cours. Vérifié visuellement 390px/1440px sur staging.
- **Dons réguliers : automatiser les prélèvements mensuels récurrents** (terminé 2026-08-26, PR #107 dev, PR #110 promotion prod) : nouvelle page `/admin/dons-reguliers` (entrée sidebar sous "Dons") pour gérer des engagements de don récurrent (participant, montant, jour de prélèvement, activité optionnelle, dates début/fin) avec une section "Dons à confirmer" listant tous les mois manquants depuis la date de début de chaque engagement actif — jamais d'insertion automatique sans confirmation admin (contrainte fiscale Cerfa). Nouvelle table `dons_reguliers` + colonne `dons.don_regulier_id` (RLS avec bypass super-admin inclus dès le départ, migration rejouée sur prod). Ajouté en cours de route sur demande de l'utilisateur : bandeau "Dons réguliers en attente de confirmation" sur le tableau de bord (`DashboardPage.tsx`), même pattern que le bandeau existant des demandes d'adhésion. Vérifié de bout en bout sur staging (backfill 4 mois, confirmation partielle, Arrêter un engagement).

**Promotion PR #110 (2026-08-27)** : `dev`/`main` réalignés en une seule PR (`gh pr create`/`gh pr merge --merge`, `dev` conservée) plutôt qu'un cherry-pick par sujet — les 4 PR dev (#106/#107/#108/#109) sont montées telles quelles, aucune divergence à gérer. Dump prod pris avant migration (`~/dump_prod_avant_promotion_20260827.sql`, supprimé après succès), migrations `activites_super_admin_bypass.sql` puis `dons_reguliers.sql` rejouées et vérifiées présentes sur prod, `generate-recu` redéployée sur prod. CLI Supabase relié à `mothana-staging` après coup (lien temporairement basculé sur prod le temps des migrations).

- **Animation d'ouverture des sidepanels mobile (Dons + Participants)** (terminé 2026-08-30, PR #111 dev) : le panneau de détail mobile ("slide over") de `DonsPage.tsx` et `ParticipantsPage.tsx` apparaissait instantanément, sans transition. Fix : réutilise exactement le pattern déjà en place dans `Toast.tsx` — state `mobilePanelVisible` + `useEffect` déclenchant `setTimeout(10ms)` au montage du panneau (uniquement quand la sélection passe de `null` à une valeur, pas en changeant de sélection) + classes `translate-x-full`/`translate-x-0` + `transition-transform duration-200`. Pas d'animation de sortie, comme cadré. Vérifié via Playwright (390px, staging) sur les deux pages.
- **Pilote shadcn/ui — reconstruction visuelle ActivitesPage** (terminé 2026-08-31, PR #112 dev) : parti d'une reformulation en cours de session de "Audit mobile colonnes" — l'utilisateur a exprimé le souhait de migrer toute l'app vers shadcn/ui (mobile-first, plus soigné). Analyse faisabilité/coût faite en chat (aucune fondation shadcn existante, 24 pages, dette non factorée documentée dans `DESIGN.md`) → décision : pilote isolé sur une seule page avant tout rollout, plutôt qu'un big-bang. Fondation posée (`src/lib/utils.ts`, `src/components/ui/{button,dialog,input,label}.tsx`, tokens namespacés `paper`/`ink`/`stamp` + polices `registre`/`registre-mono` dans `tailwind.config.cjs` — namespacés pour ne pas impacter les 23 pages non migrées). `ActivitesPage.tsx` entièrement reconstruite, direction "carnet tamponné x registre" choisie et affinée avec l'utilisateur sur plusieurs itérations (skill impeccable — tirage de direction, puis 3 corrections utilisateur : cachet redéfini comme fait dérivé des dates réelles plutôt qu'un statut de workflow inventé après vérification du vrai schéma `Activite`, police mono restreinte aux données tabulaires seulement, sections actives/à venir vs terminées séparées). Compteurs réels dons/participants par activité ajoutés au passage (nouveauté factuelle, absente avant ce chantier). Fonctionnalité 100% préservée, testée de bout en bout sur staging (Playwright, cycle CRUD complet). Revue de finition du skill impeccable (2 tours : contraste AA, anneau de focus, canvas papier, hiérarchie du cachet par statut — tous résolus). `DESIGN.md` racine non modifié (reste la référence pour les pages non migrées) — nouvelle direction tracée dans une fiche de surface dédiée (`.impeccable/surfaces/`). Bug corrigé au passage (trouvé en testant, sans rapport direct) : `tailwind.config.js` en ESM empêchait le serveur de dev **permanent** de recharger les nouveaux tokens sans redémarrage (jamais fait) — renommé en `.cjs`. **Décision prise le 2026-08-31** : généralisation validée par l'utilisateur ("on peut les attaquer 1 par 1, avec une PR à chaque fois") — voir carte Trello "Généraliser shadcn/ui aux 23 pages restantes" (Todo, suivi de la progression page par page).
- **Rollout shadcn/ui — DonsReguliersPage** (terminé 2026-08-31, PR #113 dev, 2e page du rollout, 2/24) : même direction "carnet tamponné x registre" et mêmes primitives que le pilote, aucun nouveau composant nécessaire. Différence assumée : le statut actif/arrêté d'un engagement est une vraie action manuelle de l'admin (bouton Arrêter/Réactiver), rendu en badge texte sobre plutôt qu'en cachet — le cachet reste réservé aux faits purement dérivés de dates, pour ne pas diluer son sens. Fonctionnalité 100% préservée (dons à confirmer avec montants éditables, CRUD engagement, toggle statut), testée de bout en bout sur staging. Seam assumé hors scope : `ParticipantAutocomplete`/`ActiviteAutocomplete` (composants partagés, pas encore migrés) restent dans l'ancien système visuel à l'intérieur des nouvelles modales.
- **Rollout shadcn/ui — DemandesAdhesionPage** (terminé 2026-08-31, PR #114 dev, 3e page du rollout, 3/24) : 1ère page à vrai `<table>` — nouvelle primitive `Table` (`src/components/ui/table.tsx`) + `Textarea`, réutilisables pour les 5 autres pages à tableau restantes. Nouveaux tokens sémantiques `warning` (ambre, doublon détecté) et `success` (vert registre, ratification) dans `tailwind.config.cjs`, distincts de l'encre de marque `stamp` — même principe que l'ancien `DESIGN.md` (couleurs réservées à la signalisation d'état, jamais décoratives), contraste AA vérifié par calcul (5,0:1 et 6,2:1 sur blanc). Fonctionnalité 100% préservée (onglets, détection de doublons, détail, refus avec motif, ratification via `AdherentModal` inchangé). Bug corrigé dans la même PR (trouvé en testant) : la modale de détail débordait horizontalement à cause de Téléphone/Courriel en colonnes 50% — passés en pleine largeur (Civilité/Date de naissance restent groupées, formats courts fixes) + `break-words` en défense. Seam assumé hors scope : `AdherentModal` (593 lignes, partagé) reste dans l'ancien système visuel.

**Point de vigilance permanent** : les Edge Functions ne se déploient jamais automatiquement au push/merge sur ce projet — toujours `supabase functions deploy <nom>` explicitement après une édition de leur code (voir mémoire persistante).

**Limitation connue (non bloquante, décision utilisateur)** : les 4 fonctions RPC d'import échouent en mode super-admin "Consulter" (pas de `profils_organisation` dans ce contexte) — se connecter en admin normal pour tester les imports. Même limitation confirmée le 2026-08-22 sur les Edge Functions reçus fiscaux (`generate-recu`, `send-recu-email`) : contrôle d'accès basé uniquement sur `profils_organisation` de l'utilisateur authentifié, sans tenir compte de `viewingOrgId`. Décision explicite de l'utilisateur : laisser tel quel plutôt que de faire évoluer le contrôle d'accès. Détail : `docs/journal-avancement.md`.

### ⏳ Backlog actif — ordre Trello (board "Mothana", voir mémoire persistante)

Le board Trello est la source de vérité unique du backlog (hors cartes "Action admin", gérées par l'utilisateur lui-même). Chaque carte cadrée a sa description Trello à jour avec le détail complet des décisions — vérifiée à chaque session, pas dupliquée ici. Confirmation explicite à redemander avant de démarrer le dev de l'une d'entre elles, même déjà cadrée.

Ordre reclassé le 2026-08-20, complété le 2026-08-21 et 2026-08-22 (jugement agent, mix importance/complexité — pas un simple reflet de l'ordre d'ajout) :

Passe complète de revérification/cadrage effectuée le 2026-08-20 (voir détail des décisions dans chaque carte Trello) :

1. **Audit mobile : masquer les colonnes redondantes avec la vue détail** (Dons, Participants) — cadré le 2026-08-23, cadrage d'audit (décisions colonne par colonne à trancher en dev, comme la revue UI responsive PR #57/#58). `DonsPage.tsx`/`ParticipantsPage.tsx` (les deux avec un `DetailPanel` déjà en place) n'adaptent pas leurs colonnes sur mobile alors qu'un clic sur une ligne montre déjà le détail complet. Technique retenue : `hidden md:table-cell` sur les colonnes non essentielles, pas de bascule en liste de cartes (cohérent avec la convention `ScrollShadowX`). Autres pages (Adhérents, Activités, Reçus fiscaux, Super-admin — modale d'édition, pas vue détail) laissées hors scope. [Trello](https://trello.com/c/QH2dLvfT)
2. **Support multi-organisation pour un compte admin** (rattachement + sélecteur) — cadré le 2026-08-22, détecté en testant la PR #73 (branding emails) : `create-admin` échoue avec l'erreur brute de Supabase Auth si l'email correspond à un compte déjà admin d'une autre organisation. Le schéma (`profils_organisation`, `unique(utilisateur_id, organisation_id)`) prévoit ce cas mais rien dans l'appli ne l'exploite — `AuthContext.fetchOrganisationId` fait un `.single()` qui casserait la connexion si un compte avait plusieurs lignes. Déjà listé "hors périmètre MVP" dans `docs/cadrage-mothana.md`. Décisions : sélecteur d'organisation permanent dans le header (`AdminLayout.tsx`), confirmation explicite obligatoire avant de rattacher un compte existant à une nouvelle organisation (évite qu'une faute de frappe d'email donne accès aux données d'une organisation à la mauvaise personne), email "nouvel accès" informatif (pas de lien d'invitation, la personne a déjà un mot de passe). Besoin surtout pratique pour l'utilisateur (recette/démos) à ce stade, pas de vrai besoin client confirmé — version simple. **Dev reporté à plus tard (décision explicite utilisateur le 2026-08-22)**, contourné en testant avec un email différent. [Trello](https://trello.com/c/WtLrSLGW)
3. **Vraie documentation utilisateur (guides / FAQ)** — détectée le 2026-08-21 en scindant la page de tour produit `/decouvrir` (terminée, voir "✅ Terminé"), pas encore cadrée. Piste évoquée par l'utilisateur : format FAQ plutôt qu'un mode d'emploi linéaire, avec recherche par mot-clé. [Trello](https://trello.com/c/5pwUxgqA)
4. **Créer et maintenir des `agents.md` calqués sur les `CLAUDE.md`** (pour pouvoir switcher entre Codex et Claude Code) — carte détectée le 2026-08-21, pas encore cadrée. [Trello](https://trello.com/c/0XmhK0iv)
5. **Mire de connexion personnalisée par organisation** (admin + bénévole) — cadré le 2026-08-10, revérifié 2026-08-20 (toujours aligné). Même mécanique que le formulaire d'adhésion (header/footer/css éditables, URL `/connexion/{slug}` et `/login/benevole/{slug}` réutilisant le slug existant). Valeur UX/confiance multi-org. [Trello](https://trello.com/c/gkOuH3uh)
6. **Pièces jointes sur un don** (justificatifs) — cadré le 2026-08-09, revérifié 2026-08-20 (toujours aligné). Table `dons_fichiers`, bucket Storage privé + URL signée, images/PDF 10 Mo max, upload côté admin (`DonModal`) et bénévole (`BenevolePage`). [Trello](https://trello.com/c/G1MFP7Ao)
7. **Double opt-in email** avant ratification d'une demande d'adhésion — cadré le 2026-08-08, revérifié/complété 2026-08-20 (prérequis Resend levé, point de duplication `sendViaResend` à trancher au dev). Complexité un peu plus élevée que les items précédents (nouveau token/route publique). [Trello](https://trello.com/c/0gt0LIVU)
8. **Modèles réutilisables pour les campagnes mailing** — cadré le 2026-08-20 : table `mailing_templates` par organisation (sujet + corps, pièce jointe exclue), action "Enregistrer comme modèle" dans `CampagneMailingPage.tsx`. [Trello](https://trello.com/c/BfHOUb3v)
9. **Switches d'activation de fonctionnalités par organisation** (Dons / Adhérents, espace super-admin) — cadré le 2026-08-21 : nouvelle colonne jsonb `fonctionnalites_activees` sur `organisations` (extensible, Coupon Pagode s'y ajoutera plus tard), checkboxes dans la modale d'édition d'organisation existante (`OrgModal`). Blocage = UI + garde de route (redirection si URL directe), pas de RLS — pas une frontière de sécurité. Désactiver "Dons" masque aussi Comptabilité + onglet bénévole "Saisir un don" + widgets dashboard liés ; désactiver "Adhérents" masque aussi l'onglet bénévole "Vérifier un adhérent" + Paramètres > Adhérents + widgets dashboard liés. Complexité modérée (plusieurs points de garde à câbler), pas urgent. [Trello](https://trello.com/c/1tNWFQr9)
10. **Suppression d'organisation : étape d'archivage intermédiaire** (extraction avant suppression définitive) — cadré le 2026-08-22, complexité modérée, plusieurs décisions à valider avant dev : nouvelle colonne `archived_at` (aucun statut de cycle de vie n'existe aujourd'hui sur `organisations`), le bouton "Supprimer" devient "Archiver" (réversible, bloque la connexion des admins/bénévoles de l'org, nouvel onglet "Archivées"), puis "Extraire les données" (CSV multi-tables) + "Supprimer définitivement" (réutilise la confirmation nominative PR #81/#86) depuis cet onglet. Format d'export exact et mécanisme de blocage de connexion (RLS vs applicatif) laissés ouverts au moment du dev. [Trello](https://trello.com/c/Z0kThckV)
11. **Sélection d'un adhérent à la saisie d'un don + doublonnement** — prio basse (dit explicitement par l'utilisateur), hors scope V1 du module Adhérents. Revérifié 2026-08-20 (toujours pas de lien en base adherents↔profils_participant) : cadrage volontairement resté léger vu la priorité, un cadrage détaillé complet restera nécessaire avant tout dev (mécanisme de dédoublonnage non trivial). [Trello](https://trello.com/c/RtRY8ltX)
12. **OCR scan de carte adhérent** — pas encore cadré, discussion préliminaire seulement, intégration API OCR externe, besoin niche déjà partiellement couvert par la recherche nom/prénom (livrée). [Trello](https://trello.com/c/zVBOjAWk)
13. **Priorité 5 — Export comptable enrichi** (recadré 2026-08-20, ex-"Export FEC") — un vrai FEC (partie double, plan de comptes) serait un chantier disproportionné vu l'absence totale de modélisation comptable dans Mothana ; objectif ramené à enrichir le CSV dons existant. Roadmap lointaine, besoin concret toujours non confirmé. [Trello](https://trello.com/c/W3GCYUOt)
14. **Priorité 5 — Brique événements/coupons + portefeuille virtuel** (Pagode Coupon) — clarifié 2026-08-20 : précommande/prépaiement en ligne pour les événements de la pagode (dimensionner les achats, avance de trésorerie), deux mécanismes retenus (coupon = droit à un article précis, ET portefeuille virtuel = solde rechargeable). Un projet séparé existe déjà (Supabase "pagode-coupon", en pause), pas encore inspecté — prérequis avant cadrage technique. [Trello](https://trello.com/c/C9A5B9jr)
15. **Priorité 5 — Gestion abonnements/plans** (facturation Samakan) — clarifié 2026-08-20 : facturation SaaS de Samakan lui-même (les organisations clientes paient un abonnement), pas une fonctionnalité pour les donateurs des associations. Aucun modèle économique décidé à ce jour, roadmap lointaine. [Trello](https://trello.com/c/cfKF8BNw)

**Carte supprimée du board le 2026-08-26** (constatée disparue en début de session, hors session Claude Code, décision utilisateur de ne pas la recréer) : "Ajouter des contraintes de saisie de mot de passe dans la page reset password".

**2 cartes archivées le 2026-08-25** (besoin déjà couvert / fusionné, pas de dev séparé) : "Mailing : envoyer à des listes personnalisées" (fusionnée dans la carte 1 ci-dessus) et "Cerfa : éditer les cerfa des années précédentes" (le sélecteur d'année permet déjà de générer/régénérer jusqu'à 3 ans en arrière).

**Carte clôturée le 2026-08-20** (besoin déjà couvert, pas de nouveau dev) : "Contrainte d'unicité `id_externe` par organisation sur les activités" — la contrainte existe déjà en prod (`activites_organisation_id_externe_unique`, migration `import_id_externe_unique_constraints.sql`), confirmé par requête directe sur la base. Déplacée en Done sur Trello.

---

## Instructions pour Claude Code

1. **Lire ce fichier en entier** avant toute action
2. **Chercher le fichier de session du jour** dans `.claude/sessions/` avant de commencer
3. **Suivre `docs/brief-cerfa.md`** pour la priorité en cours
4. **Mettre à jour "État d'avancement"** après chaque étape complétée — un résumé court dans `CLAUDE.md` (un item de la liste "✅ Terminé"), le détail complet (décisions, bugs trouvés en testant) va dans `docs/journal-avancement.md` plutôt que dans `CLAUDE.md`, pour garder ce fichier léger
5. **Sauvegarder un résumé de session** dans `.claude/sessions/` en fin de session
6. **Ne jamais sauter d'étape** sans validation explicite
7. **Demander confirmation** en cas de doute fonctionnel ou technique
8. **Demander confirmation explicite avant de démarrer le dev** d'une carte ou d'un sujet, même déjà cadré — ne pas enchaîner automatiquement après le merge d'une PR précédente (règle renforcée par rapport au défaut global, sur ce projet on redemande systématiquement, pas seulement en cas de doute sur la fraîcheur de l'accord)

Règles génériques valables sur tous les projets ("un sujet à la fois", ne pas présumer qu'un accord ancien tient toujours, continuité entre sessions) : voir `~/.claude/CLAUDE.md`.